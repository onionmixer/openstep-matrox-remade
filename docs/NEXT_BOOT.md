# 다음 부팅에 할 일 (2026-08-29 02:45 설치본 기준)

이 문서는 **재부팅 직후 몇 분 안에** 무엇을 하고 무엇을 읽어야 하는지만
적는다.  설계 근거는 M4·M5·M6 에 있다.

## 0. 설치본

```
/private/Drivers/i386/OpenStepMGAReplacementDisplay.config/
    OpenStepMGAReplacementDisplay_reloc      511,984 바이트, 02:45
git HEAD 와 일치 (빌드·설치를 커밋 뒤에 다시 했다)
```

## 1. 절차

```
1  ./tools/nx-mount.sh                       (/ndrv2 로 붙는다)
2  bash tools/nx.sh "wc -l /private/adm/messages"      <- 시작 줄수를 적어 둔다
3  cc -O -o /me/wq /ndrv2/openstep-matrox-remade/test/openstep-mga-warpqual-probe.m \
        -lDriver -ObjC
4  /me/wq
5  awk 'NR>N' /private/adm/messages           <- N 은 2 에서 적은 값
```

`OSMGAWarpQual` 은 **로드된** 드라이버를 돌린다.  부팅 로그가 아니라 이 경로로
읽어야 25 줄이 다 남는다(부팅 중에는 syslogd 가 아직 안 뺀다).

## 2. 이번에 새로 나오는 밴드

| 밴드 | 묻는 것 | 읽는 법 |
|---|---|---|
| **T7a/b/c** | WARP 좌표에서도 축마다 감기는가 | 감김이면 **히스토그램 1 개 빈에 전부**, 클램프면 흩어지고 정확일치가 전부.  **잡아둔 축이 틀리면 무조건 실패** |
| **T7e** | 선형 이음매가 사다리꼴과 같은가 | `M1_4CB §8-2` 의 `u 127 / v 128 / 모서리 127` 과 대조.  `0` 이나 `255` 면 이웃 탭이 클램프된 것 |
| **T8a** | 어느 알파가 목적지에 닿는가 | 첫 밴드가 `0..255` 램프, 둘째(선택자 0)가 `0x33` 이어야 한다 |
| **T8b** | 두 경로가 같은 그림으로 블렌딩되는가 | `covered` 가 0 이 아니고 `differ` 가 0.  기대 블렌드 `8f604f`(엔진 `/255`) — 프리필 `00204060` 도 소스 `80FF8040` 도 아니어야 한다 |
| **T9b/T9a** | 런 사이 펜스가 필요한가 | run A 가 깊이를 **하나도** 안 갖고 run B 가 갖는지.  두 밴드가 같으면 **펜스 불필요**, 펜스 있는 쪽만 맞으면 필요 |

## 3. 결과가 정하는 것

```
T7 통과  ->  osmgaHW3DWarpAdmits 에서 REPEATU/REPEATV 거절을 푼다
T8 통과  ->  같은 함수에서 블렌딩 ALPHACTRL 거절을 푼다
T9       ->  M6 §4 의 런마다 펜스를 넣을지 뺄지 확정한다
```

셋 다 **한 함수와 한 문단**만 바꾼다.  그렇게 되도록 미리 갈라 두었다.

## 4. 실패했을 때

`*** REBOOT REQUIRED ***` 가 찍히면 그 지점에서 멈춘 것이다.  하네스는
**일부러** `stormBusy` 를 잡은 채 두고 링과 마이크로코드를 유지한다 — 카드가
그 주소를 갖고 있고 읽기를 멈췄다는 증거가 없기 때문이다.  그 뒤로는 재부팅
말고 할 것이 없고, **그 이전 밴드의 결과는 이미 로그에 있다.**

T9a(펜스 없는 밴드)가 그렇게 될 가능성이 가장 높고, **그것 자체가 답이다.**

## 5. 하드웨어가 필요 없는 것은 이미 다 했다

```
계약      버전 10 WARP 배치 (v9 레이아웃 불변, 재빌드 불필요)
검증기    구조 + 정점 비트 + 상태 + 정책 함수 하나
유저랜드  정점 변환기 + 배치 조립기
결합      조립기 출력을 커널 검증기에 먹이는 시험
정리      드라이버의 유일한 부동소수 제거 (67,600 상자 동등성)
스위트    열한 개, 타깃 cc 전부 통과
```

남은 M6(제출 경로)은 **T9 의 답이 구조를 바꾸므로** 그 전에 쓰지 않는다.
