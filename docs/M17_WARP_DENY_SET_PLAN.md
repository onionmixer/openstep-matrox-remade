# M17 — WARP 이 거절해야 하는 것 (2026-08-29, 코딩 전)

`M16 §11` 이 정한 계약: **백엔드 선택은 관측 가능해서는 안 된다.**
`M16 S4-e` 가 그 계약을 어기는 사례를 하나 찾았다.

## 1. 사실

얼어붙은 슬리버(crossfind #2), 훅이 받은 좌표로:

```
경로         화소   소프트웨어와
소프트웨어     4     --
사다리꼴       4     일치     (TRICROSS 로 거절하고 되돌린다)
WARP           2     3 개 빠지고 1 개 더 있음
```

기하:

```
크기        1.3984 x 87.0273 화소
넓이        1.0526 px^2
넓이/둘레   0.0060 화소   (평균 두께의 절반)
넓이/외접   0.0086
```

사다리꼴이 "맞는" 이유는 **되돌림**이다.  그 층도 이 삼각형을 직접 그리지
않는다.

## 2. 정해야 할 것

WARP 을 이런 기하에서 떼어 놓는 규칙.  두 제약이 있다:

* **사다리꼴 빌더를 돌리게 하면 안 된다** — 그러면 WARP 의 셋업 이득 일부를
  버리고, 그 층 구현의 우연에 WARP 의 범위가 묶인다(`M16 §11`).
* **이기는 프리미티브를 잃으면 안 된다** — 측정된 이득이 3.5~26% 다.

## 3. 후보

```
(1) 넓이 문턱        2*|area| < T 면 거절.  교차곱 하나
(2) 두께 문턱        area / 가장 긴 변 < T.  슬리버에 더 충실
(3) v9 의 검사를 공유  복제가 아니라 진짜 함수를 부른다
```

(3) 은 codex 가 복제보다 낫다고 한 쪽이지만, 그것이 곧 §2 의 첫 제약을
어긴다.  (1) 은 유저랜드 삼각형당 교차곱 하나 — WARP 이 피하려는 바로 그
종류의 일이지만 아주 작다.

## 4. 문턱을 **재서** 정한다 — 하나의 슬리버에 맞추지 않는다

이것이 계획의 중심이다.  사례 하나로 규칙을 만들면 그 하나에 맞춘 규칙이
나온다.  저장소는 v9 에 대해 이미 옳은 방법을 썼다: `crossfind.c` 가 **진짜
빌더와 진짜 검증기를 몰아** 거절되는 삼각형을 찾았다.

같은 방법을 신탁에 대고 쓴다:

```
근퇴화 삼각형을 넓은 범위로 만든다 (넓이·두께·종횡비·방향을 쓸어서)
각각을 WARP 으로, 그리고 소프트웨어로 그린다
어긋나기 시작하는 곳을 찾는다
문턱은 그 경계에서 나온다 — 슬리버 하나에서가 아니라
```

경계가 넓이가 아니라 다른 양(두께? 종횡비?)에 있다면 그것이 규칙의 모양을
정한다.

## 5. codex 에 물을 것

1. 문턱을 **재서** 정하는 이 방식이 옳은가, 아니면 근퇴화 기하에서 "경계"
   자체가 잘 정의되지 않아 다른 접근이 필요한가?
2. 규칙이 유저랜드 정책(`osmgaMesaWarpTakes`)에 있어야 하나, 커널 검증기
   (`osmgaHW3DWarpAdmits`)에 있어야 하나?
3. 잘못 거절될 **옳은** 얇은 삼각형이 있을 텐데(사다리꼴로 가므로 그림은
   맞다), 그 비용을 어떻게 재나?
4. 넓이·두께 말고 이 불일치를 더 잘 예측하는 양이 있나?
5. 빠뜨린 것.

---

## 6. codex 교차검토 판정

| codex 주장 | 검증 | 결과 |
|---|---|---|
| **"경계" 가 경계가 아닐 수 있다** — 근퇴화 기하에서 나쁜 집합은 `양 < T` 가 아니라 **떨어진 섬들**일 수 있다.  더 얇은 삼각형이 표본을 하나도 안 덮어 우연히 맞을 수도 있다 | 논리 | ✅ **채택.**  쓸기를 **특성화**로 쓰고 규칙 선택의 근거로 쓰지 않는다 |
| 판정은 **변환 뒤**에 있어야 한다 — 정책 선검사는 `Hook.c:1287`, 정점 변환은 `:1313` 이라, 들어온 좌표에 건 규칙은 **실제로 제출된 삼각형과 다를 수 있다** | 두 자리 확인 | ✅ **채택.  결정 지점을 옮긴다** |
| 커널에 두면 안 된다 — 그쪽 정책은 **런 단위**이고 `badRun` 만 보고하므로, 커널 거절은 **조립된 런/배치 전체를 오염**시킨다.  게다가 FPU 가 없다 | 코드 확인 | ✅ **채택.  유저랜드가 의미 라우팅을, 커널이 격리 검증을** |
| 비용이 "얇은 삼각형 몇 개" 보다 훨씬 크다 — 거절마다 **쌓인 WARP 를 플러시**하고 층 전환을 강제하고 런을 조각낸다.  `M15` 의 churn 163% 가 그 증거다 | 자기 측정과 대조 | ✅ **채택.  거절률만으로는 부족하다** |
| 거절된 WARP 삼각형이 사다리꼴이 아니라 **소프트웨어**로 갈 수도 있다 — 사다리꼴도 `TRICROSS` 를 거절하므로 | 사실 | ✅ **채택.  내가 뭉뚱그렸다** |
| `2\|A\|/Lmax`(최소 높이)가 넓이보다 나은 첫 불변량 | python: 이 슬리버는 **0.0242 화소** | ✅ **채택** |
| **관용된 색·깊이 차이가 공통 바닥의 일부인가?  명시하지 않으면 "백엔드 선택은 관측 불가" 는 이미 시험 불가능하게 강하다** | 논리 | ✅ **가장 날카롭다.  §7 이 계약을 고친다** |
| 규약 결함일 수도 있다 — 병진·감기·꼭짓점 순열을 먼저 시험하라 | **실측** | ✅ **채택하고 돌렸다** (§8) |
| "간단한 규칙이 없다" 도 유효한 결과여야 한다 | 논리 | ✅ **채택** |

## 7. 계약을 고친다 — 관용치는 계약의 일부다

`M16 §11` 은 "백엔드 선택은 관측 가능해서는 안 된다" 고만 적었다.  그런데
이 프로젝트는 이미 **측정되고 문서화된 차이 셋을 받아들인다.**  그대로 두면
계약이 스스로와 모순되고 시험할 수가 없다.  고쳐 적는다:

> 받아들여진 그리기는 **선언된 층 관용치 안에서** 소프트웨어와 일치해야
> 한다.  관용치는 열린 면허가 아니라 이 셋이고, 이 셋뿐이다:
>
> ```
> 색     한 레벨 낮음                        (M12 §8)
> 깊이   한 코드 + (|dz/dx|+|dz/dy|)/16      (M12 §9, M13)
> 소유권 변에 붙은 화소의 귀속               (M12 §3)
>        -- 단 커버리지는 정확하다: mesh16 은 gap 0, extra 0 이고
>           움직인 것은 승자뿐이었다
> ```

슬리버는 이 셋 **밖**이다.  소프트웨어가 그리는 화소를 **비워 둔다** —
귀속이 아니라 **커버리지**가 틀렸고, 그것은 선언된 어디에도 없다.

## 8. 규약 결함이 아니다 — 재서 배제했다

규칙을 설계하기 전에, 이것이 정밀도 한계가 아니라 **넘겨주는 방식**의
문제일 가능성을 닫아야 한다.

```
병진   하위 화소 오프셋 15 개    15/15 전부 불일치
순열   여섯 꼭짓점 순서(양 감기) 6/6 전부 동일하게 불일치
       (소프트웨어는 여섯 순서에서 같은 집합을 그린다 — 순서 무관)
```

**위상 사고도, 감기 규약도, 꼭짓점 순서도 아니다.**  기하 자체다.

## 9. 그러므로 다음은 규칙이 아니라 특성화다

codex 가 승인한 것은 쓸기를 **특성화**로 쓰는 것이지 스칼라 문턱을 뽑는
것이 아니다.  그리고 다음 셋을 먼저 정해야 한다:

```
분류기의 목표      무엇을 분리하려는가
위상 완비 행렬     하위 화소 위상·감기·방향·크기·높이·클리핑을 층화
결정 지점          정점 변환 뒤 (Hook.c:1313 이후)
혼합 층 비용 예산  얼마를 넘으면 그 규칙은 출하하지 않는가
"간단한 규칙 없음" 도 유효한 결과
```

**비용 기준을 재기 전에 적는다**: 대표 부하에서 plain 이득(3.5%)을 규칙이
잡아먹거나, 거절이 만든 조각남이 섞인 장면을 되돌림 기준선보다 나쁘게
만들면, **전역 WARP 정책으로 출하하지 않는다.**

---

## 10. 특성화 쓸기 (2026-08-29) — **슬리버 이야기가 아니었다**

`test/sliver-gen.py` 가 근퇴화 삼각형 480 개를 층화해 만든다(높이 8 x 길이
3 x 각도 5 x 위상 4).  기하는 python 이 만들고 그리는 쪽과 판정하는 쪽이
같은 파일을 읽는다 — `mesh-gen.py` 의 방식.

WARP 대 소프트웨어:

```
480 개 전부 WARP 이 받았고, 342 개 일치, 138 개 불일치 (28.8%)

높이별   0.005:17/60  0.01:19/60  0.02:18/60  0.05:19/60
         0.1:14/60    0.2:13/60   0.5:16/60   1.0:22/60
각도별   0:11/96   22.5:62/96   45:4/96   67.5:59/96   90:2/96
길이별   10:22/160   40:45/160   87:71/160
위상별   0:26/120  0.25:28/120  0.5:48/120  0.75:36/120
```

**높이는 예측하지 못한다** — 가장 두꺼운 1.0 화소가 가장 나쁘다(22/60).
"슬리버라서" 가 아니다.  **각도가 강하게 예측한다** — 축 정렬(0·90)과
대각(45)은 거의 멀쩡하고 **비스듬한 각도(22.5·67.5)가 나쁘다.**  길이가
길수록 나빠진다.

**어느 축도 두 집합을 가르지 못한다**: 불일치 집합에 나타나는 값이 전부
일치 집합에도 나타난다.  그러므로 **이 축들 위의 문턱은 옳게 그리는
삼각형을 거절하고 틀리게 그리는 것을 받아들인다.**  codex 가 예고한
"떨어진 섬" 이 맞다.

## 11. 그리고 대조군이 죽었다 — **출하 중인 층의 크래시**

같은 가족을 사다리꼴 층에 돌리자 **SIGFPE 로 죽었다**(RC=136).
WARP 과 소프트웨어는 480 개를 완주한다.

하나로 좁혔다.  네 가지가 **동시에** 필요하다:

```
높이  <= 0.005 화소   (0.02 는 정상)
위상  정확히 0.5      (밑변이 표본 행 위에 딱)
각도  0 (수평 밑변)   (22.5·45·67.5·90 은 정상)
길이  >= 40 화소      (10 은 정상)
```

재현하는 삼각형:

```
(140.5, 120.5) (180.5, 120.5) (160.5, 120.505)

1/256 단위로   35968,30848   46208,30848   41088,30849
  -> 밑변 두 꼭짓점의 y 가 정확히 같다: 밑변 변의 높이가 0
  -> 꼭대기는 한 스텝 위: 30849 - 30848 = 1
```

위상 0.5 만 죽는 이유는 행 산술이다:

```
위상 0.25  밑변 30784 꼭대기 30785  rT 120 rL 120  -> 아무것도 안 만든다
위상 0.50  밑변 30848 꼭대기 30849  rT 120 rL 121  -> 사다리꼴을 만든다
위상 0.75  밑변 30912 꼭대기 30913  rT 121 rL 121  -> 아무것도 안 만든다
```

**유저랜드 크래시다** — 프로세스만 죽고 기계는 멀쩡하며, 고치는 데
재부팅이 필요 없다.  그러나 **합법적인 GL 삼각형에 출하 중인 층이 죽는
것**이고, 이것이 이 회차에서 나온 가장 심각한 것이다.

M17 의 원래 질문(WARP 의 거부 집합)보다 **이것이 먼저다.**
