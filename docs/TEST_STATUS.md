# Current Verification Status

기준일: **2026-08-24** (3D 행은 2026-08-24 갱신)

> **읽는 법.** 이 표는 시간순 원장이다. `R6-WORKING` 행부터가 현재 실기에서
> 동작하는 `OpenStepMGAReplacementDisplay`의 사실이고, 그 위의 P1/P2/P3/R1~R4
> 행은 **탐색 과정의 기록**이다. 특히 P2 MiG control plane과 lease/sidecar
> 서비스는 **폐기된 경로**이며 현재 드라이버는 그 코드를 링크하지 않는다
> (빌드 대상은 `OpenStepMGAManualConfig.c` + `OpenStepMGAEDID.c`뿐).
> 아직 하지 않은 일은 `REMAINING_WORK.md`에 따로 있다.

| 영역 | 최신 결과 | hardware 접근 | 상태 |
| --- | --- | --- | --- |
| P1 PCI/config inventory | existing `MatroxMGA` 및 PCI header read-only 확인 | PCI config header read-only | 통과 |
| P1.4 capability header probe | bounded standard capability header walk; VPD presence marker only | PCI config header read-only | host gate·target build/load/log 통과; IDs `01`,`02`, VPD absent; G2 미통과 |
| P1.2 display metadata | `Display0`, mode index/count; VRAM getter uninformative | 없음 | 통과, VRAM gate 미해결 |
| P1.3 subsystem/VRAM reconciliation | 16 MiB profile vs `0d43` 32 Mb catalogue label을 미확정으로 보존 | 없음 | 통과, P3 차단 유지 |
| R0 production baseline | current `MatroxMGA` owner/configuration, `/ndrv` mount, original bundle size/mode/`sum` fingerprint (`R0-20260818-A`) + target-native exact comparator (`R0-20260818-B`) | target read-only | baseline/current-boot comparator 통과; original boot/recovery cold-boot rehearsal 미시작 |
| R1 staging isolation | source fail-closed table/probe, production directory artifact absence, Configure/`InstanceN.table` snapshot model, original two-table field snapshot plus exact P-original collector (`R1-20260818-C`), i386 recovery-staging package and isolated P-recovery template | target read-only + `/tmp` package build | archive/BOM, staging-only payload, `/private` install rejection, import allowlist and template/package-separation pass. P-original exact ID/location/mode 및 replacement absence pass; P-recovery/P-failure Configure snapshots와 Installer rollback evidence가 없으므로 G1 sole-owner configuration은 미통과 |
| R1 recovery snapshot matrix policy | P-original/P-recovery/P-failure exact 1/0, 0/1, 1/0 candidate invariant + bundle/table/rollback/atomicity/recovery evidence | target process memory only | host C89/static gate와 target `cc` pass. Installer rollback 및 P-failure original boot을 independent required flags로 검증; synthetic fixture only. P-recovery/P-failure가 없으므로 G1 sole-owner configuration은 미통과 |
| G1 hardware precondition | PCI G450 primary-head, fixed conservative 16 MiB deployment input, 1600×1200×32/162 MHz record | 없음 | operator-directed 완료. 이 결정은 live VRAM probe/physical maximum/mapping/mode programming 또는 sole-owner recovery snapshot을 증명하지 않음 |
| H1 hardware interrogation decision | operator가 display owner를 generic SVGA(IOVGADisplay)로 전환·MatroxMGA 미로드; 단일 telnet 복구 + VGA fallback + 최대 로깅; intel1000식 `kl_util` probe 채택; staged S0→S3 인터로게이션 승인 | 결정 문서 | `docs/H1_HARDWARE_INTERROGATION_DECISION.md` 기록. R6(owner test/mode programming) 이후 게이트는 불변 |
| H1 S0 live PCI fingerprint | VGA 소유 상태에서 `OpenStepMGAProbe` read-only 재실행: `04:00.0` vid=102b did=0525 rev=85 class=030000 irq=11, command=0007 status=0290, BARs `f8000008/e8200000/e8800000`, cap `01`/`02`, VPD absent | PCI config read-only (kernel loadable) | target load/run/unload clean; BAR base 확정(FB `0xf8000000` prefetchable, MMIO `0xe8200000`, ILOAD `0xe8800000`). aperture 크기(S1)·VRAM 실측(S3)은 미측정 |
| H1 S1 BAR aperture sizes | config-space write-1s→mask→restore-verify. BAR0 base `0xf8000000` size `0x02000000`(**32 MiB** FB, prefetch=1), BAR1 base `0xe8200000` size `0x00004000`(**16 KiB** MMIO control), BAR2 base `0xe8800000` size `0x00800000`(**8 MiB** ILOAD) | PCI config write+restore (command reg 미변경, MMIO/VRAM 미접근) | 세 BAR 모두 restore-ok(mismatch 없음), command-unchanged=0007. FB aperture 32 MiB ≥ 16 MiB 배치이므로 매핑 여유 확인. 이는 aperture(주소창) 크기이며 populated VRAM(16 vs 32 MiB)은 S3에서 실측 필요 |
| H1 S2 MMIO read-only characterization | `IOMapPhysicalIntoIOTask`로 BAR1(`0xe8200000`,16KiB) read-only 매핑→X.Org 검증 오프셋 read→VCOUNT 8샘플→unmap. dwgctl=`0`, fifostatus=`0x210`, status=`0x80820000`, opmode=`0`, memctl=`0`. VCOUNT `0x236`→`0x271` 200µs마다 +7~8 단조증가 | MMIO read-only (write 전무, config 불변, Reset/EXEC 미접근) | bare loadable 매핑 성공(virt `0x21446000`). VCOUNT 단조증가로 **uncached+aperture 디코드+live scanout** 증명(증가율이 800×600@60 스캔라인 주기와 일치). command-unchanged=0007, VGA 화면 무영향, 클린 unmap. 오프셋은 G400/G450 공통(`R6_G400_G450_REGISTER_DIVERGENCE.md`). populated VRAM(S3) 미측정 |
| R1 G450 P-recovery configuration admission | extracted recovery-table strings의 exact driver/function/mode/16MiB/profile validator | target process memory only | host C89/static gate와 target `cc` pass; target review-template read-only check도 pass. recovery table value mutation fail-closed; Configure table read/write, bundle load, mapping/display action은 미시작 |
| R2 physical-profile source audit | family envelope, board inspection run sheet, `0d43`→G45FMDVP32DSF 32 MB DDR candidate, target original bundle software sweep (`R2-20260818-A`) | target file read-only | exact physical P/N/max-capacity remains separate research. Operator confirmed the PCI G450 16 MiB minimum deployment limit, so it no longer blocks the conservative 16 MiB release profile |
| R2.1 BSD/Linux/original auto-detection research | FreeBSD legacy DRM, X.Org MGA 2.0.0, Linux MGA UAPI, public MatroxMGA distribution | host/offline only | adapter/topology auto-identification path 확인; X.Org VRAM count는 map/register/VRAM write probe, FreeBSD DRM은 caller-provided layout consumer. Existing `MGA Memory Size`는 override이므로 G2 미통과 |
| R2.1a manual memory configuration | C89 parser + replacement configuration-table hook for `MGA Memory Size` | host + target `/tmp` C test; replacement i386 clean-build/import inspection | static analysis of target-original binary confirms original-compatible nonzero 3..63 MiB input range. Original catalogue separately proves G400 8 MiB profile and explicit G400/G450 16 MiB key; it has no 32 MiB table. Missing/invalid/out-of-range fail closed; host/target test and import gate pass. R4 remains unmatchable/unloadable and configured value is not physical-profile evidence |
| R2.1b original binary configuration audit | target-sum-matched local `MatroxMGA_reloc` Ghidra static analysis, reproducible Java xref/decompile scripts | local analysis copy only | original `MGA Memory Size` override accepts 3..63 MiB after original probe; `MGACountRam`/BIOS selector path is hardware-sensitive. No replacement probing/mapping is authorized; G2 remains unpassed |
| R2 profile admission policy | pure-C board/cross-check/VRAM type+size/RAMDAC evidence validator with nonempty evidence references | 없음 | host C89/static gate 통과; current candidate/configuration 값은 valid profile을 만들 수 없으며 G2 미통과 |
| R5 recovery rehearsal | original-driver-only cold boot `R5-20260818-A`, target-native pre/postflight, outside-sandbox telnet recovery, NFS remount, original fingerprint/artifact checks, operator GUI confirmation | target read-only + cold reboot | PASS; G4 통과. replacement driver install/load/probe는 여전히 미시작 |
| R5 recovery rehearsal (VGA 기준선) | `R5-VGA-20260818-A`: H1 VGA-known-good 기준선에서 operator-driven cold reboot 후 복구 루프 실증. telnet 재접속 ≤60s, Active Drivers=`VGA` 유지, Workspace GUI 정상 기동, /ndrv·gcdsd·logcatch·커널로그 회수 스크립트 재현 | target read-only + cold reboot (same-config) | PASS. 이 복구 절차를 드라이버 활성화 복구 경로로 채택. "telnet이 깨진 display 부팅에서 생존" 가정은 미증명(OPENSTEP telnetd 독립성에 근거). 근거 `docs/reports/R5_VGA_RECOVERY_REHEARSAL_RUN_SHEET.md` |
| P2.7 source safety gate | hardware API/device binding/MiG surface 거부 | 없음 | host 통과 |
| P2.8 binary import gate | strict 8-import allowlist | `nm -u` read-only | target 통과 |
| P2.9 clean reproducibility | NFS clean-copy/build, P2.8, full P2 suite, cleanup | P2 MiG control-plane만 load | target 통과 |
| D0 EDID parser/policy | source purity guard, C89 loader probe/parser/unit/import tests; manual/fixed-table gate | 없음 | host 통과; architecture/runtime variants와 P2-namecheck-equivalent import target binary가 모두 `main()` 전 loader failure; target claim 보류 |
| D0.1 mode footprint policy | pure-C 8/16/24/32 bpp pitch/overflow/profile-limit arithmetic | 없음 | host C89 통과; physical VRAM limit을 추정하지 않음 |
| R3 current mode arithmetic | target manual mode parse + 1600×1200×32 lower-bound footprint | 없음 | host C89 통과; offscreen range/G2 proof에는 사용하지 않음 |
| R3 one-mode review policy | approved 16 MiB PCI G450 deployment profile + exact manual-memory equality + embedded complete timing shape + pitch/mapping bounds + pure-C footprint validator | target process memory only | `R3_G450_16M_DEPLOYMENT_MODE` host/target C89 record pass. 1600x1200@60 DMT is the sole first replacement design mode; target mode programming remains unstarted |
| R3 timing-shape policy | active/porch/sync/polarity/clock total과 exact integer refresh를 검증하는 pure-C record | target process memory only | host C89/static gate와 target `cc` pass. 1600x1200@60 DMT comparison fixture의 162 MHz/2160x1250 결과만 검증하며 target blanking/head/timing evidence는 미확정 |
| R3 16 MiB render budget | reviewed byte inputs로 scanout/reservation/color/depth/pitch footprint를 합산하는 pure-C policy | target process memory only | host C89/static gate와 target `cc` pass. 1600x1200x32는 color-only까지, 1024x768 double-color+16-bit depth는 arithmetic comparison only; G2/G3/P3 및 target allocation은 미시작 |
| P3 fixed-target presentation oracle | 1024x768 fixed render target과 same-size/CPU nearest-neighbor presentation contract | target process memory only | host C89/static gate와 target `cc` pass. ordinary-memory scale oracle only; real scanout write, hardware scaling, driver connection은 미시작 |
| P3 Mesa backend/fallback selector | fixed 1024x768x32+depth16 target, R3/R6 record-derived render-budget/ownership/mapping/fence gated hardware candidate, mandatory OSMesa fallback | target process memory only | host C89/static selector/admission gate와 target `cc` pass. synthetic 16 MiB record의 fence-missing path는 fallback을 선택한다. installed Mesa 3.4.2 OSMesa 1024x768 context/clear/pixel smoke도 `/tmp`에서 pass; driver/device/display action은 미시작 |
| D0.2 P3 admission policy | five independent evidence gates; first blocked gate reporting | 없음 | host C89 통과; current software evidence stops at `VRAM_SIZE` |
| D1 replacement ownership | `IOFrameBufferDisplay` lifecycle, sole-owner boot/recovery, D2/D3 gate design | 없음 | 문서 설계 완료; target bundle/source load 미시작 |
| R4 replacement skeleton | fail-closed `IOFrameBufferDisplay` subclass, source gate, target i386 compile/import allowlist | 없음 | host gate, target build, target automatic import gate 통과; R6 transaction/offscreen policy linkage도 explicit reject. install/load/probe 미시작 |
| R6 step1 driver foundation | R4 skeleton을 실드라이버로 진화: Default.table PCI 매칭(`Bus Type=PCI`, `Auto Detect IDs=0x0525102B`, Memory Maps FB `0xf8000000`/MMIO `0xe8200000`/VGA/BIOS), `+probe:` live PCI 검증, init에서 chip 분류(rev→G450/G400 path), manual memory, `mapMemoryRange:` FB(range0, writethrough)+MMIO(range1, cache off) 매핑 골격 | 없음 (target build only; 미설치·미활성화) | target 클린 빌드·링크 통과(reloc 87564B, 경고 없음). G400 PLL 경로·활성화 reboot 미시작 |
| R6 step2 driver mode programming | (구판) 프로젝트 byte-image 기반 최소 모드셋 — 코드 리뷰(codex+독립 fork)로 **검은 화면 확정**: CRTC 0x17 미설정·DAC 픽셀포맷/VGA enable/PAN_CTL 누락·G450 PLL raw write 오류 | — | 아래 재작성판으로 대체 |
| R6 step2b driver full X.Org modeset | (중간판) X.Org 완전 모드셋 재작성 후 실기 활성화 시도 → **부팅 hang**(FB 매핑) 및 **transaction이 60Hz 정확검증으로 실모드 거부** 발견 | — | 아래 R6-WORKING으로 대체 |
| **R6-WORKING 실기 동작** | 원본 MatroxMGA IDA+codex 교차분석 반영: init에서 **FB를 `mapFrameBufferAtPhysicalAddress:length:`**로 매핑(raw IOMapPhysicalIntoIOTask는 WindowServer hang), BAR0/BAR1 런타임 read, empty Memory Maps, EDID.c 링크(미링크시 심볼 미해결→로드 조용히 실패). enterLinearMode: **transaction 제거, 원본 직접 프로그래밍** — blank(SEQ reset+screen off)→PLL(clksel 0x0c+lock search)→PAN_CTL→DAC(skip 0x2c-2e/4c-4e)→CRTCEXT→Misc/SEQ/CRTC/GR/ATTR→**선형 팔레트(PIX_RD_MSK/WADR_PAL/COL_PAL)**→CRTCEXT0 relatch→unblank+PAS video-on→IODelay(10ms)→FB clear | **실기 활성화·cold reboot** | ✅ **PASS — G450 1024×768×32 TrueColor 정상, WindowServer/Workspace/WM 풀 GUI 동작**(2026-08-19). 핵심 수정: FB 매핑 메서드, EDID.c 링크, transaction 제거(실모드 60.003Hz≠정확60), 팔레트 로드 |
| **R6 다중해상도·다중심도** | `osmgaRes`(5개: 640×480~1600×1200 VESA DMT) × `osmgaFmt`(RGB:888/32, RGB:555/16, RGB:256/8 PseudoColor+`setTransferTable:`, BW:8/4 grayscale) 테이블화. `osmgaComputeCRTC`가 `bppShift` 인자로 CRTCEXT3/stride 계산, DAC 루프가 포맷별 MUL_CTL override, 팔레트 분기(RGB/gray=선형 또는 양자화 램프, PseudoColor=윈도서버 컬러맵). config `Display Mode` 문자열로 res+format 선택, `Display.modes` 25모드(5×5)로 Configure.app 노출 | **실기 활성화·cold reboot** (1024×768 32bpp 회귀, BW:4, RGB:555/16 각각) | ✅ **PASS**. RGB:888/32 회귀(mnp 동일), BW:4(gray-quantized, 8bpp 신경로 실증), RGB:555/16(1600×1200, PLL 재작성 후 정상) 모두 화면 확인. RGB:256/8·BW:8은 8bpp 경로 공유로 사실상 검증(별도 부팅 안 함). 2bpp/32bit-gray는 G450 스캔아웃 한계로 불가 확정([[openstep-matrox-depth-support]]) |
| **R6 G450 픽셀 PLL 재작성** | 원본 `G450SetPLLFreq`을 IDA로 완전 해독(후보생성 `sub_4628`/`sub_4534`, 정렬 `G450CompareMNP`, 지터밴드 ±1/±2/±3 안정성 검사+fallback)해 충실 포팅. 기존 코드는 단일 최소오차 후보+첫락 즉시반환이라 1600×1200에서 marginal MNP(`000400`, VCO 324MHz)를 채택해 간헐적 signal error 유발. codex 교차검토로 해독 검증 | **실기 활성화·cold reboot** (1600×1200 RGB:555/16) | ✅ **PASS**. 후보 9/30(`mnp=021000`) 채택, 1~8위(고VCO 포함)는 실제 지터검사 탈락 — 설계대로 동작. 정상 밝기+정상 sync. 정본 `docs/R6_G450_PIXEL_PLL_ALGORITHM.md`. 다른 해상도(1024×768 등)는 이 변경 이후 재부팅 검증 안 함(operator 판단) |
| **R6 15bpp 팔레트 수정** | RAMDAC가 15bpp에서 5비트(0-31)만 인덱싱하는데 256엔트리 항등 램프를 로드해 최대색 31→31/255(12%) 매핑, 화면이 어두웠음. 원본 `setGammaTable`(32엔트리 `byte_6984[8*i]`)과 대조해 확정, 32엔트리를 0-255 전범위(`i*255/31`)로 로드하도록 수정 | **실기 활성화·cold reboot** (1600×1200 RGB:555/16) | ✅ **PASS**. 정상 밝기 확인(PLL 수정과 별개 버그였음) |
| **R6 dead-code 정리** | `osmgaReadCrtcExt`/`rollbackTransaction:` 제거. 파생 조사로 드라이버 `.m`이 애초에 `profile/*.h`를 import 안 하는 것 발견 — H1 트랜잭션 시대 잔재로 13개 모듈(profile/ 11개+protocol/ BoundedPoll·ModeTransaction)이 완전 무참조 상태로 링크만 되고 있었음. 저장소 전체 grep으로 다른 서브시스템(`OpenStepMGAModeTransaction.h`, `mesa/`) 사용 확인 후 소스는 유지, 드라이버 Makefile/빌드스크립트 링크만 제거 | target 빌드만(재부팅 불요 — 순수 죽은 코드 제거) | ✅ **PASS**. reloc 261524→132256 bytes, 빌드 경고 0, `nm -u` 미해결 심볼 0 |
| **S1 Storm 2D 엔진 생존** | OpenGL 가속 1단계. 오프스크린 64×64 단색채우기 1회 + CPU readback. X.Org 2.0.0에서 유도한 레지스터 시퀀스(`DWGCTL=0x000C7804` TRAP\|SOLID\|ARZERO\|SGNZERO\|SHIFTZERO\|RPL\|bop_copy, `MACCESS=2`, 13-write 상태초기화). 안전장치: config 플래그 기본 OFF, 32bpp·실증VRAM(7MiB)·매핑범위 3중 가드, `CXBNDRY`/`YTOP`/`YBOT`를 블록으로 좁힌 하드웨어 클립, 모든 대기 bounded(타임아웃시 EXEC 미실행·정리write 금지), sentinel 선기록으로 거짓성공 배제, readback은 `IOMapPhysicalIntoIOTask` **uncached 별칭**으로(FB 매핑의 읽기 캐시속성 미증명) | **실기 활성화·cold reboot** (1024×768×32) | ✅ **PASS**(2026-08-19). `fifo depth=16`(H1 S2 실측과 일치), `sentinel ok`, 4096px 전부 `0xDEADBEEF`, checksum `0xDBEEF000` **독립 재계산 일치**, 가시화면 무변화(육안), telnet 생존. **프로젝트 최대 미지수 제거 — 유도 시퀀스에 엔진이 실제 반응함.** 정본 `docs/S1_STORM_ENGINE_LIVENESS_PLAN.md` |
| **R6 잔여** | G400 PLL 알고리즘(`MGATi3026SetPCLK:bpp:` 계열, G400 실물 보드 미확보로 미착수) | — | 미착수 |
| **S2 Storm BITBLT** | OpenGL 가속 2단계 = **프레젠테이션 원시연산**. 위험을 둘로 분리: **S2a** 오프스크린→오프스크린(소스 주소지정 의미론 `AR3`/`AR0`/`AR5`/`SGN` 전부, 화면 위험 0), **S2b** 오프스크린→가시 우하단(목적지 주소만 새로움, S2a PASS시에만 런타임 실행). `DWGCTL=0x040C4008`(AtypeNoBLK[GXcopy]=RPL\|bop_copy \| SHIFTZERO \| BITBLT \| BFCOL). 소스를 단색이 아닌 **위치 인코딩 패턴**(`0xFF000000\|row<<8\|col`)으로 채워 어긋난 복사·오프셋 오류까지 검출. 클립은 **목적지 전용**으로 좁힘 | **실기 활성화·cold reboot** (1024×768×32) | ✅ **PASS**(2026-08-19). `source pattern ok`, S2a 4096px→(0,1152) 일치, S2b 4096px→(960,704) 일치, 화면 이상 없음. **S1도 같은 부팅에서 재PASS**(공유 헬퍼 리팩터 무회귀, checksum 동일). 정본 `docs/S2_STORM_BITBLT_PLAN.md` |
| **S3a `IODisplayDoBlit` 구현** | OpenGL 가속 3단계. **방향 전환**: MiG 서비스 확장 대신 `driverkit/displayDefs.h`의 표준 블릿 API를 구현 — 유저스페이스는 `IODeviceMaster setIntValues:`로만 접근하고 MMIO를 못 봄, 엔진은 드라이버 단독 소유, 기존 P2 가드 무관, 유저태스크 매핑 불필요. 신규: 겹침 방향 처리(memmove 규칙→`SGN`/`AR5`/`start,end`), 입력 전수 검증, `stormBusy`+`simple_lock` 직렬화(경합시 대기 없이 `IO_R_RESOURCE`), **EXEC 후 타임아웃은 가속 영구 비활성화**(엔진이 아직 쓸 수 있어 SW 폴백이 덮이므로). `IO_DISPLAY_CAN_BLIT` **미광고** | **실기 활성화·cold reboot** (1024×768×32) | ✅ **PASS 6/6**(2026-08-19): 비겹침/`BLIT_UP`/`BLIT_LEFT`/**대각(양방향)**/`src==dst` no-op/잘못된입력 4종 거부. 위치인코딩 패턴이라 방향 오류시 즉시 검출됨. 정본 `docs/S3_IODISPLAY_DO_BLIT_PLAN.md` |
| **S3b-prep 계측·측정** | 락을 설계하기 전에 **사실을 측정**. 카운터 19종(`getIntValues` `"OSMGAStats"`, `AMD_SCSI` 선례대로 exact-count 검증), 커서 3종 오버라이드(카운트+`super`만, 락·할당·로그·대기 전무 — 인터럽트 문맥 가정), 테스트 전용 `"OSMGAProbeBlit"`(표준 `IODisplayDoBlit`을 계약 위반하며 쓰지 않기 위해), 유저스페이스 프로브 `test/openstep-mga-stats-probe.m`(`-lDriver`). **`mach_kernel` IDA 정적분석**으로 `IOFrameBufferDisplay` 커서 3종 디컴파일 | **실기 부팅 + 유저스페이스 호출** | ✅ **미지수 4개 전부 해결**(2026-08-19): ①유저스페이스→커널 RPC 실증(`Display0`→object 22) ②커서는 **소프트웨어 커서**(IDA: `frameBuffer` 대상 CPU 복사 루프, `ev_try_lock` 비차단·경합시 생략) ③커서 **실제 호출됨**(`cursorMove 103`) ④**좌표 원점 top-left 확정**(변환 불필요). S3a self-test 6/6 회귀 없음. 정본 `docs/S3B_PREP_INSTRUMENTATION_PLAN.md` |
| **S3b reject-only 측정** | `IO_DISPLAY_CAN_BLIT`을 광고하되 모든 표준 요청을 **하드웨어 접촉 전 기록+거절**(`"Storm Blit Observe"="Yes"`). 엔진 미사용이라 위험 0 | **실기 부팅 + 정상 사용** | ❌ **`IODisplayDoBlit`은 OPENSTEP 4.2에서 사문화 확정**: `cursorMove 1784`로 활발히 사용됐는데 `blitRequests 0`. 바이너리 확인 — WindowServer·mach_kernel 어디에도 `DoBlit` 문자열 없음(대조: `IOSetTransferTable`·`IO_Framebuffer_Map` 등은 존재). **WindowServer는 프레임버퍼를 직접 매핑해 CPU로 그린다.** → S3b 성립 불가. reject-only로 확인해 **위험 0으로 사실 획득** |
| **S4a VRAM→유저태스크 매핑** | Mesa 렌더타깃이 VRAM에 있어야만 가속이 성립(엔진은 VRAM만 조작)하므로 S4의 전제. `+[IODevice addToCdevswFromDescription:...]`로 문자 디바이스 등록(major 자동할당, 유저는 `IOCharacterMajor` 조회), `d_mmap`은 **불변 상태 기반 순수 산술**(2차 호출에 `-1` 검사가 없어 가변 플래그 사용 시 `0xFFFFF000`이 물리주소가 됨), 창=`[4MiB,7MiB)`=가시영역 위 256행 가드~실증VRAM. 호스트에서 경계 14케이스 전수검증. `OSMGAProbeFill` 신설(창 안 전용 엔진 채우기 — 없으면 stale-cache 테스트 불가) | **실기 부팅 + 유저스페이스 mmap** | ✅ **전항목 PASS**(2026-08-19): mmap OK, 유저 R/W PASS, **stale-cache 게이트 COHERENT**(캐시 오염 후 엔진 쓰기가 보임), UNTOUCHED PASS, 가시스캔아웃·8MiB 모두 REFUSED. S3 self-test 6/6 회귀없음. **플랫폼 사실 확정: 이 커널 VM 페이지는 8 KiB(`PAGE_SHIFT=13`), mmap은 4.2BSD판(호출자가 `vm_allocate`한 주소 위에 덮어씌우고 성공시 0 반환, `MAP_FIXED` 없음)**. 정본 `docs/S4A_VRAM_MMAP_PLAN.md` |
| **OpenGL 가속 후속(S4b)** | 호출자를 WindowServer가 아닌 **Mesa**로. S3a 자산은 그대로 유효 — 유저스페이스→커널 RPC 실증됨, 엔진 구동 경로 완성, 좌표 원점 확정. 매핑 경로는 S4a로 **확보 완료**. 남은 선행: ①오프스크린 할당자 + per-client 소유권(`d_mmap`은 클라이언트 신원을 못 받으므로 검증된 minor 등에 인코딩 필요) ②매핑 수명(언로드해도 매핑이 살아남음 — 현재는 언로드 금지로 대응) ③창 3 MiB로 색상+깊이가 들어가는지(1024×768×32 색상만 3 MiB) → populated VRAM 실측이 관건 | — | 미착수 |
| R6 DriverKit mapping API audit | local `IOFrameBufferDisplay`/`IODirectDevice` headers와 S3/QVision sample lifecycle 대조 | 없음 | `mapMemoryRange`/matching `unmapMemoryRange`를 future recovery-only candidate로 문서화. range/BAR/cache/mode 값은 미확정이며 R4 gate 유지 |
| R6 mapping configuration policy | R3 record + sole-owner/recovery/range/cache evidence + bounded range index/length validation | 없음 | host C89/static gate 통과; synthetic fixture only. mapping API call·target configuration·G1/G2 verdict는 미시작 |
| R6 Storm 2D source audit | local X.Org 2.0.0 `mga_storm`/register/FIFO/DRI separation review | 없음 | 2D bring-up order와 no-DMA/bounded-wait/offscreen-first boundary 기록. MMIO helper·register write·target access 미시작 |
| R6 G450 PLL/mode source audit | local X.Org 2.0.0 PLL/DAC paths + OPENSTEP display lifecycle comparison | 없음 | exact-timing/one-head/bounded-lock/one-candidate rollback transaction으로 분리. offline CRTC/PLL data encoders exist; target hardware code·access는 미시작 |
| R6 G450 offline PLL plan | R3 physical/timing review + explicit head/source evidence를 요구하는 pure-C frequency candidate arithmetic | 없음 | host C89/static gate 통과; abstract divisor/frequency output only. approved offline fixture이며 DAC/PLL register programming·G1/G2 verdict는 미시작 |
| R6 G450 PLL byte image | one reviewed frequency plan을 primary pixel-PLL C 또는 secondary video-PLL M/N/P byte-image로 C89 encoding | target process memory only | host C89/static gate 및 target integrated transaction test 통과. 162 MHz primary image `00/04/00` 확인; DAC index/data I/O, live candidate retry, mode transition은 미시작 |
| R6 G450 range-publication plan | approved 16 MiB framebuffer + VGA/BIOS legacy ranges를 synthetic base에서 exact three-entry data plan으로 생성 | target process memory only | host C89/static gate 및 target integrated transaction test 통과. DriverKit range publication/physical mapping은 미시작 |
| R6 G450 CRTC geometry plan | approved 1600×1200@60 R3 record에서 display/sync/total/pitch/scanout을 checked arithmetic으로 생성 | target process memory only | host C89/static gate와 target `cc` pass. separate data-only primary image exists; hardware register write, mapping, DAC/PLL/CRTC action은 미시작 |
| R6 G450 primary CRTC byte image | checked 32-bit R3 record를 primary VGA/extended CRTC image 및 MiscOut external-clock bit-mask로 offline encode | target process memory only | host C89/static gate와 target integrated transaction `cc` pass. approved bytes `00=09,01=c7,04=cf,06=e0,10=b0,11=23,13=90,16=e1`; VGA/DAC/MMIO write·display state capture·mapping은 미시작 |
| R6 G450 primary CRTC readback comparator | caller-provided CRTC/extended/MiscOut snapshot을 approved image와 pure-C로 compare | target process memory only | host C89/static gate와 dedicated target `cc` test pass. CRTC/extended는 exact, MiscOut은 requested external-clock bits만 검사; snapshot acquisition, VGA/DAC/MMIO read/write, state capture는 미시작 |
| R6 original G450 mode list | installed `MatroxMGAG450_16MB.modes`의 1600×1200@60 RGB:888/32 selection text | target original bundle read-only | target count `1` pass. mode-selection list이며 register image/range/cache/head evidence는 아님 |
| R6 bounded poll policy | elapsed-time/ready sample only의 terminal timeout·stable-ready state machine | 없음 | host C89/static gate 통과; PLL/FIFO register reader와 미연결이며 timeout values도 미설정 |
| R6 one-mode transaction | R6 mapping + complete G1 recovery matrix, identical R2 evidence references, checked 32-bit G450 CRTC geometry + primary byte image, PLL review, bounded lock, required CRTC snapshot comparison, reported linear result, four-stage terminal rollback composition | 없음 | host C89/static gate와 target `cc` test 통과; dual-owner/incomplete recovery matrix, CRTC image/plan omission·16-bit substitution은 preflight에서 거부. snapshot mismatch는 rollback으로 전환하며 display/PLL/VGA-safe/superclass-revert restoration report가 모두 없으면 completion도 거부. R4 bundle과 미연결이고 mapping/mode/DAC/PLL/CRTC target action은 미시작 |
| R6 offscreen 2D admission | active transaction + allocator-live kernel-owned/outside-scanout opaque surfaces + clear/copy geometry | 없음 | host C89/static gate와 target `cc` test 통과; synthetic 64×32 surfaces only. submit ABI, target allocation, MMIO/engine action은 미시작 |
| R6 offscreen allocation ledger | verified opaque arena, aligned 32-bit footprint, opaque ID ownership, live-record check, no-reuse-before-fence policy | 없음 | host C89/static gate와 target `cc` test 통과; synthetic byte/slot capacity, alignment, stale/mutated record rejection 확인. target allocator/mapping/address/offset/MMIO action은 미시작 |
| R6 target pure-C validation | transaction, offscreen allocator, reference oracle를 `/ndrv` source로 target `cc` compile/run | target process memory only | transaction(reference-identity/manual-memory-equality revision)/allocator/reference 모두 pass. `/tmp` binary cleanup 완료; device/driver/display action 없음 |
| P3 clear/copy reference oracle | caller-owned memory의 bounded rectangle clear와 distinct-surface copy | 없음 | host C89 통과; padding preservation/self-copy rejection 확인. hardware fallback 또는 target memory access 없음 |
| next replacement-driver stage | R0~R7 plan + immediate A~E execution package, evidence artifacts, rollback/stop conditions | 없음 | R4 및 Work package A 완료; next evidence work는 R5 original-only recovery rehearsal과 별도 R2 physical inspection; target configuration 및 hardware action 미시작 |
| P3 reference oracle | clear/checksum/compare, flat triangle winding, 16-bit depth, source-alpha blend, nearest clamp texture | 없음 | host C89 통과; hardware submit/readback 미시작 |
| P3 command envelope | 32-bit surface/clear/flat-triangle geometry, overflow/range/degenerate rejection | 없음 | host C89 통과; P2 MiG ABI와 hardware submit 미연결 |
| **정리·`HAS_TRANSFER_TABLE` 측정** | 관측(observe) 모드 전면 제거, 통계 19종으로 정리(`setTransferTable` 카운터 신설), 죽은 코드 제거(`chipVendorDevice`/`chipRevision`/`manualVideoMemoryConfigured`/`OSMGA_ENABLE_MODE_PROGRAM`/`MGA_BPP32_SHIFT`/`OSMGA_RANGE_*`), 설정 기본값을 `Storm 2D Test`=No·`VRAM Mmap`=No로 정직화하고 `Default.table`에 문서화. `IO_DISPLAY_CAN_BLIT` 광고 중단 → `IO_DISPLAY_HAS_TRANSFER_TABLE` 광고 | **실기 부팅 + 유저스페이스 호출** | ✅ **인과 확정(2026-08-19)**: 같은 모드(1024×768 RGB:888/32)로 플래그 **미광고 상태 7회 부팅 동안 `setTransferTable` 호출 0회**, 플래그 **광고 후 첫 부팅에서 1회**(`setTransferTable cached (256 entries, RGB:888/32)`). 즉 **WindowServer는 `IO_DISPLAY_HAS_TRANSFER_TABLE`을 보고서야 컬러맵을 보낸다.** 부작용: `RGB:256/8`(PseudoColor)에서는 이제 WindowServer 컬러맵이 RAMDAC에 **실제로 기록**된다(그전까지는 우리 리니어 램프가 그대로 남았음) — **실기 검증 완료**(2026-08-19, 1024×768 `RGB:256/8` 부팅): `setTransferTable live colormap (256 entries)` 기록, 화면 색 정상. codex 교차검토 완료(도달불가 프로브 분기 1건 지적·수정) |
| ~~P3 3D path~~ | — | — | **이 행은 오래됐다.** 3D 는 시작됐고 아래 행들이 현재 상태다 |
| **3D 커버리지 규칙** | 화소 중심 표본 + top-left 채우기. 독립 기하 신탁(python 정수 연산)에 대고 양 경로를 채점 | **실기 부팅 + 가속 실행** | ✅ 시험 도형 전부 **하드웨어가 규칙과 정확히 일치**. 소프트웨어와의 행별 차이 0. 빈틈·겹침·방향 의존 없음. 근거 `M1_4A2_COVERAGE_RESULT.md`, `M1_4A3_RASTER_RULE_PLAN.md` |
| **3D 변 걷기 규칙** | 엔진이 실제로 어떻게 걷는지 (`a = AR1 − AR2`, 0 행은 FXBNDRY 자리, 행 사이에 `a < 0` 인 동안 배수) | **실기** | ✅ 3 방향 탐침으로 확정(8/8, 20/20, 대조군). 앞서 "확정"이라 적은 규칙은 `AR1 == AR2` 제약 아래의 **동치류**였다 — `M1_4A8_EDGE_WALK_GAP_PLAN.md` |
| **3D 색·알파 보간** | 평면을 화소 **중심**에서 읽고, 출력 절단을 반올림으로 보정 | **실기** | ✅ 두 도형·네 채널. 소프트웨어와 **99.6% / 99.3% 화소 동일**, 남은 차이는 1 단계(동률 반올림). `M1_4A5_ATTRIB_FIX_PLAN.md` |
| **3D 깊이 보간** | 화소 중심 정착, 시작값 클램프 제거(빈 첫 행 건너뛰기) | **실기** | ✅ 중심 잔차 ±2662 코드 → **−0.5**(출력 절단만 남음). 클램프 발동 **0**(계기로 확인). `M1_4A7`, `M1_4B2_DEPTH_CLAMP_PLAN.md` |
| **3D 정점 좌표** | `1/256` 고정소수점 전달 + 삼각형마다 실을 수 있는 만큼 소수부를 AR 인코딩에 싣기 | **실기** | ✅ 부동소수 잡음(행 하나가 통째로 밀림) 제거, 소수 기하 **5.01% → 0.21%**. 정수 정점은 **한 화소도 안 변함**(호스트 410 삼각형·98640 행). `M1_4A9`, `M1_4B1_SUBPIXEL_PLAN.md`, `spec/subpixel-model.py` |
| **3D 텍스처** | 좌표·업로드·상주·선택자, 그리고 **원근**(`q ≠ 1`) | **실기** | ✅ `GL_RGB`·`GL_RGBA`, `GL_REPLACE`·`GL_MODULATE`, `GL_NEAREST`·`GL_LINEAR`, 감싸기 셋. 장면 기준선 14 종이 회귀를 잡는다. 밉맵은 조사를 마치고 닫았다 — 비트가 이 경로에서 무력하다(`M1_4D9`), 그래서 `MinFilter` 에서 거절한다 |
| **3D 공유 변** | 이웃 삼각형이 변을 공유할 때의 틈·겹침 | **실기** | ✅ 색·깊이 모두 **이음매 없음**. fan·strip·512 삼각형 메시까지 (`M1_4B4` §9, `M1_4B6`) |
| **3D 깊이 비교** | 일곱 비교를 각각 세 질문(멀다·같다·가깝다)으로 | **실기** | ✅ 일곱 전부 소프트웨어와 일치(`tdf`). `GL_NEVER` 는 거절 — `zmode` 1 은 Matrox 해독기가 **예약값**이라 적은 것이지 NEVER 가 아니다 |
| **3D 깊이 쓰기 (`glDepthMask`)** | 접근 유형이 그것이다 — ZI 는 비교하고 쓰고, **I 는 비교만** | **실기** | ✅ 원시 프로브: ZLT 로 가까운 띠 1280/1280 그림, 먼 띠 0, **깊이 이동 0**. 같은 행을 NOZCMP 로는 전부 그리므로 기하가 아니라 비교다. GL 쪽은 증인 사각형으로 확인(`tdm`), 투명 패스 포함 |
| **3D 블렌딩** | GL 1.1 의 소스·목적지 인자 전부와 엔진의 산술 | **실기** | ✅ 인자 전부 소프트웨어와 두 단계 이내(`tbf`). 산술은 262144 표본으로 적합 — 곱 두 번, 더하고, **한 번** 반올림. 장면 4 종이 정확 일치 |
| **3D 알파 시험** | 어느 알파를 비교하는가, 그리고 버린 프래그먼트의 깊이 | **실기** | ✅ 일곱 전부(`tat`). 비교하는 것은 **텍스처 스테이지의** 알파이고(양방향 쓸기가 200 에서 만남), 버린 프래그먼트는 깊이도 안 쓴다 |
| **3D 폴리곤 오프셋** | `factor`·`units` | **실기** | ✅ python 과 정확히 일치(`tpo`) |
| **3D 시저** | 엔진의 목적지 클립(`CXBNDRY`/`YTOP`/`YBOT`) | **실기** | ✅ 4928 중 정확히 768, 경계 아홉 점·빈 상자·표면 밖·부분 겹침 전부 소프트웨어와 화소 일치(`tsc`). 처음에는 정반대로 보고했는데 **시험이 시저를 켠 채 지워** 직전 프레임을 물려받고 있었다(`M1_4E7`) |
| **`GL_SRC_ALPHA_SATURATE`** | 엔진 소스 인자 8 이 GL 의 분리를 나르는가 | **실기** | ❌ 아니다. 다섯 행 전부 **색에 `SRC_ALPHA`, 알파에 `ONE`** 이고 `255-Ad` 가 들어오지 않는다(`tsa`). 인자 7 대조군이 Ad 를 정확히 읽음을 보여 "목적지 알파를 못 본다"는 대안을 닫았다. **측정된 이유로 거절** |
| **`glClear` 가속** | 전면 사각형을 엔진으로 — 색만은 atype I, 색+깊이는 ZI+NOZCMP 로 한 번에 | **실기** | ✅ 512×384 에서 174.0 → **146.7 ms**. 붙인 직후 아무것도 안 달라져서 진단을 만들었더니 `verdict 7`(E_DWGCTL), 전부 0 인 삼각형 — `built[]` 를 `batch->tri[]` 로 **복사하지 않았다**(`M1_4EA`) |
| **표면 인바운드 복사** | 호출자가 이미 가지고 있던 그림 | **실기** | ✅ 미러가 한 방향뿐이라 호출자의 그림이 사라지고 있었다. 재현 10688/10688 → 고친 뒤 **0**(`tib`). 첫 바인딩·재바인딩 양쪽 |
| **후퇴 시 깊이 보존** | 가속을 놓을 때 색은 넘고 깊이는 못 넘었다 | **실기** | ✅ Mesa 할당자가 제 주석대로 초기화하지 않는다. 재현 `8000` → `0000`, 고친 뒤 `8000` → **`8000`**(`tdp`). 시험 첫 판은 후퇴가 아예 안 일어났고 그 단언이 잡았다 |

최신 full no-hardware host regression은 original-compatible manual-memory 3..63 MiB
range 정렬 후 다시 실행되었고
`OPENSTEP_MGA_NO_HARDWARE_HOST_CHECKS=pass`를 반환했다. 이 결과는 target driver
ownership 또는 R2/G2/R5/G4 verdict를 바꾸지 않는다.

P2.9 종료 뒤 `OpenStepMGAService`는 deallocated 되었고, `MatroxMGA`는
`0x2112a000` / `0x12000`으로 loaded 상태였다. P2 control-plane의
`hardware_ready=0`도 변하지 않았다.

> 2026-08-18 이후 갱신: operator가 target을 generic SVGA(IOVGADisplay) 단독
> 소유로 재부팅했다. 현재 Active Drivers에 `MatroxMGA`는 없고 로드되어 있지
> 않으며(`kl_util -s` unknown), MGA G450 native BAR region은 어떤 드라이버도
> 소유하지 않는다. 이 상태에서만 H1 staged interrogation을 수행한다. 근거는
> `H1_HARDWARE_INTERROGATION_DECISION.md`.

P3을 열기 위해서는 physical VRAM type/size, existing scanout/cursor/hidden
allocation, mapping compatibility를 각각 독립 증거로 확정해야 한다. 16 MiB profile,
32 Mb board catalogue, P2 test success 어느 하나도 이 조건을 대체하지 않는다.

세부 실행 근거는 `P1_SUBSYSTEM_MEMORY_RECONCILIATION.md`,
`P2_CLEAN_REPRODUCIBILITY.md`, `D0_EDID_PARSER_POLICY.md`,
`P2_RESOURCE_OWNERSHIP.md`, `NEXT_STEP_EXECUTION_PLAN.md`를 따른다.
| **WARP 삼각형 하나 (D2-2c/d)** | 마이크로코드가 정점을 받아 그리는가 — **1 차 DMA VERTEX 모드**(`PRIMADDRESS \| 0x3`), 파이프 7(`tgzsaf`) | **실기** | ✅ 1176 px = `48*49/2` 정확, 색 정확, **클립 밖 0**. 두 부팅 재현. 참조 구현 셋 어디에도 실행 선례가 없던 모드 — **2 차 DMA 의 네 번 NO-GO 를 우회가 아니라 제거**. 완료 규약은 `DWGSYNC`(3-139) 이지 `wbusy` 가 아니다: **기동된 WARP 는 대기 중에도 busy** 라 `!wbusy` 는 suspend 전엔 도달 불가(`D2_2C`, `D2_2D`) |
| **WARP 배치 (D2-2e)** | 한 제출이 프리미티브 여럿을 나르는가 / 파이프를 다시 먹일 수 있는가 | **실기** | ✅ 둘 다. 72 dword 에 삼각형 3 개(구분자 없음), 그리고 **새 GENERAL 목록 없이** 2 개 더 — 앞 것 무손상. 4096 px 전수 대조 wrong 0. **참조보다 강하다**: DRM 은 flush 마다 컨텍스트를 다시 낸다(`mgaioctl.c:432`, *"FIXME: Workaround bug in kernel module"*) |
| **WARP 버퍼 재사용 (D2-2f)** | 정점 버퍼는 언제 덮어써도 되는가 | **실기** | ✅ **`PRIMADDRESS` 가 지나간 순간**. 그리기 완료를 안 기다려도 된다 — 21 삼각형 2856 px wrong 0, 펜스 없는 제출이 포인터 경계에서 `dwgengsts=1`(질문이 실제로 시험됨). **배치마다 펜스 불필요.** 덤으로 **그리기 중 `PRIMADDRESS` 재기록도 안전**(참조 둘 다 안 하는 동작) |
| **`PRIMEND` 연장 (D2-2g)** | `PRIMADDRESS` 없이 `PRIMEND` 만 올려 목록을 늘릴 수 있는가 | 미실행 | ⏸ **보류**. 4-16 이 *"fill the last set with no-ops"* 를 요구하고 VERTEX 모드엔 no-op 이 없다. IDA 로 확인: Windows 는 모든 패킷의 미사용 인덱스를 `0x15`(DMAPAD)로 채우는 **GENERAL** 링이다(`W19`). 측정된 병목도 아니다. 재개 조건은 프로파일이 시작 주소 되읽기를 지목할 때 |
| **WARP 커버리지 동등성 (M3 T0)** | WARP 가 사다리꼴 경로와 같은 픽셀을 덮는가 | **실기** | ✅ 오라클 1176/0 wrong, WARP 1176/0 wrong, **둘 사이 0 픽셀 차이**. 정확 A/B 비교가 허가됨 — T1 이하의 전제 |
| **WARP 깊이 규약 (M3 T1·T2)** | z 가 `[0,1]` 인가 / `ZI` 가 WARP 로도 깊이를 쓰는가 | **실기** | ✅ 둘 다. T1 `drew 136 0 136` → **z 는 [0,1] 정규화**(코드 단위가 아니다). T2 `depth inside 136 px, min 49152 max 49152, 밖 0` → **`ZI` 가 쓴다**, 그리고 **배율은 ×65536**(0.75·65535=49151.25 가 아니다). 참조 DRI 의 `1.0/0xffff`(`mga_xmesa.c:362`)는 **틀린 쪽**이고, 변환은 `z_warp = code/65536.0` 이어야 한다 |
| **WARP 텍스처 앵커 (M3 T3)** | WARP 의 텍셀 앵커가 사다리꼴 경로와 같은가 | **실기** | ✅/⚠ 위상 1·2·3 은 **1176 화소 전부 정확**, 위상 0(텍셀 경계 정렬)만 `1 exact / 1175 wrong, du −1..0`. → **앵커가 경계 바로 아래**에 있다. 생산 규칙: **텍셀 경계에 정확히 얹어 샘플링하지 않는다**. 네 위상 밴드가 없었으면 통과로 읽혔을 오차다 |
| **WARP 층 전환 (M3 T6)** | 사다리꼴 그리기를 사이에 끼워도 WARP 가 돌아오는가 | **실기** | ✅ 1176/0 wrong — **단, 상태 전체 재발행이 조건**. 펜스만으로는 부족하고 suspend·quiesce 뒤에 다른 층을 태워야 한다 |
| **WARP 원근·클램프 (M4 T4a/b/c, T5a/b)** | 깊이가 `rhw` 로 나뉘는가 / 텍스처에 나눗셈이 있는가 / `z=1` 끝점 / `CLAMPUV` 가 확대·축소 양쪽에서 미는가 | **실기** (G450, PCI `0525102b` rev `0x85`) | ✅ 넷 다. **T4a `0 differ, worst 0 codes`** — `rhw=(1,1,1)` 과 `(1,4,2)` 의 깊이가 코드 단위로 동일(원근이었다면 최대 10923 코드 차). 아핀 램프 대비 잔차 `−1..0` = 17.15 절단분. 사양서 예고대로 **깊이는 화면공간 평면**. **T4c `z=1 → 65535`** — 감기지 않고 **포화**한다 → `code = min(floor(z·65536), 65535)`, `z = code/65536.0` 왕복 정확. **T4b `vs PROJECTIVE 1176 exact, residual 0..0`** (vs AFFINE 48 exact, `−14..0`) — **나눗셈이 있고 정수 오라클과 전 화소 일치**, `v` 도 1176 화소 전부 행 8. **T5a·T5b 각 2304 px, halo 가중치 0** — 확대(`magfilter`)·축소(`minfilter`) 양쪽, 네 변·네 모서리. 캐시 투기 읽기는 **주장하지 않는다** |
| **WARP repeat·블렌딩 (M5 T7a/b/c/e, T8a/b)** | `GL_REPEAT` 가 WARP 좌표에서도 축마다 감기는가 / 선형 이음매가 사다리꼴과 같은가 / 어느 알파가 목적지에 닿는가 / 두 경로가 같은 그림으로 블렌딩되는가 | **실기** | ✅ **넷 다**(`M9 §0` 이 측정값을 적어 놓았다: T7 `phasefail 1 1 1` — 경계 위상 하나뿐, T7e 이음매가 사다리꼴과 같음, T8a 선택자가 고름, T8b `wrong 0`).  이 줄은 밴드가 답한 뒤에도 **오랫동안 '실기 대기' 로 남아 있었다** — 2026-08-29 codex 교차검토가 이 표와 `M9` 의 불일치를 잡았다.  판정이 나오면 표를 고치는 것이 밴드를 도는 일의 일부다.  아래는 설계 근거로 남긴다: ⏳ T7 판정은 **앵커 독립**이다 — 4 텍셀/화소 sweep 이라 반 화소 차이가 2 텍셀을 움직이므로 기대 텍셀이 아니라 **모양**을 본다: 감기면 어떤 오프셋이든 히스토그램 1 개 빈에 48/48, 클램프면 32~33 개 빈에 흩어지고 정확일치 48/48. **잡아둔 축이 움직이면 무조건 실패** — 첫 초안은 물리 x 만 봤다. T7e 는 `M1_4CB §8-2` 가 사다리꼴에서 잰 이음매(u 127 / v 128 / 모서리 127)와 대조. T8a 는 `TDUALSTAGE` 두 레인을 다 쓰고 텍스처 알파 `0x33` 을 음성 대조로 둔다. T8b 는 사다리꼴과의 A/B 이고 **전용 비교기**를 쓴다(`osmgaM3CompareSurfaces` 는 알파를 버리고 아무것도 안 그려도 통과한다). 기대 블렌드는 python 으로 `8f604f`(엔진 /255) |
| **런 사이 펜스가 필요한가 (M6 T9a/b)** | 두 번째 GENERAL 목록이 앞 런의 그리기가 아직 끝나지 않은 채로 나가도 되는가 | **실기** | ✅ **불필요**.  `T9 PASS -- 펜스 불필요`, `busy 1, leak 0 0`(`M7 §`, `M9 §0`).  제출 루프에서 왕복을 뺐다.  아래는 설계 근거로 남긴다: ⏳ 판정은 **이진**이다 — run A 는 `atype I`(깊이 안 씀) 삼각형 20 개, run B 는 겹치지 않는 자리에 `atype ZI`(깊이 씀). 깊이를 0 으로 채워 두면, 두 번째 상태 목록이 안 먹었으면 run B 가 깊이를 안 쓰고, 너무 일찍 먹었으면 run A 가 쓴다. **펜스 있는 밴드를 먼저** 돌린다 — 없는 쪽이 wedge 하면 그것이 답이고 앞 결과는 이미 로그에 있다. codex 는 원리로 '필요하다'고 했고 이 밴드가 잰다 |
| **커널 v10 제출 경로 (M10)** | 커널이 클라이언트의 버전 10 배치를 실행하는가 | **실기** (다섯 시험 행렬) | ✅ 전부 통과. v9 대조군(앞·뒤) 정상, v10 dry 정상 배치가 `dwords 134`(상태 목록 110 + 정점 24), 망가진 넷이 `E_VTXCOUNT`·`E_VTXFLOAT`·`E_DWGCTL`·`E_DSTORG` 로 각자 갈리며 **인코딩 0**, 살아 있는 제출이 **`drawn 1176 of 1176, wrong 0, outside touched 0`**. 행렬이 두 결함을 잡았다 — 생산 경로가 마이크로코드 배치를 **시험에 기대고** 있었고, `warp_init` 을 **하지 않았다**(검증·인코딩은 멀쩡한데 엔진이 목록을 안 돌렸다). 살아 있는 제출 하나만 돌렸다면 둘 다 '엔진 실패' 로 보였을 것 |
| **Mesa 를 거친 첫 WARP 장면 (M12)** | 훅의 WARP 분기가 켜지면 사다리꼴과 같은 그림이 나오는가 | **실기** (`mesh16`, 512 삼각형, 꼭짓점 전부 1/256 격자 위) | ✅/⚠ **커버리지는 정확, 소유권에 56 화소**. 사다리꼴 대조군 `(안칠함 0, 넘게 0, 귀속틀림 0)`, WARP `(0, 0, 56)` — 52224 중 0.107%. 56 개 **전부** 가장 가까운 변에서 **1/16 화소 안**(중앙값 0.0083, 최대 0.0153)이고 **전부 꼭짓점 공유 이웃이 썼다**; 정확히 변 위에 얹힌 표본은 0, 무관한 삼각형은 0. → **변에 붙은 소유권**이지 커버리지도 주소 계산도 아니다. 여기까지 오는 데 결함 둘: `DWGCTL` 병합 누락(bop 0 = 불 연산 ZERO — 정확히 맞는 화소 위에 **전부 검정**), 그리고 **반 화소**(`(462, 463, 7707)`; 결합 5x5 격자의 최소가 정확히 `(+1/2,+1/2)`). `M12` |
