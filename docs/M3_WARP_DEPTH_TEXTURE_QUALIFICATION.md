# M3 — WARP 의 깊이·텍스처 자격검증 (2026-08-28, 코딩 전)

지금까지의 WARP 증명은 **전부 깊이를 끄고 텍스처 레지스터를 0 으로** 돌렸다.
M2 §11 이 이것을 진짜 blocker 로 지목했다.

## 0. 이미 확립된 것 — 다시 묻지 않는다

M2 에서 세 번 같은 실패를 했으므로, 이번엔 먼저 훑었다.
**아래는 전부 사다리꼴 경로로 실기 확인이 끝났다**(`docs/TEST_STATUS.md`):

| | 결과 |
| --- | --- |
| 깊이 비교 일곱 | 전부 소프트웨어와 일치. `GL_NEVER` 는 거절 — `zmode` 1 은 **예약값** |
| `glDepthMask` | **접근 유형이 그것이다 — `ZI` 는 비교하고 쓰고, `I` 는 비교만** |
| 깊이 보간 | 중심 잔차 −0.5, 클램프 발동 0 |
| 공유 변 | 색·깊이 모두 이음매 없음 |
| 알파 시험 일곱 | 전부 일치. 비교하는 것은 **텍스처 스테이지의** 알파, 버린 프래그먼트는 깊이도 안 쓴다 |
| `CLAMPUV` | **가져오는 주소**를 텍스처 안에 묶는다(실측). 그래서 좌표 행렬을 아예 안 묶는다 |
| 텍스처 크기 | 2 의 거듭제곱일 필요 없음. 상한 2048. 피치는 텍셀 단위 |

**이것들은 그리기 엔진의 성질이고, WARP 는 그 엔진에 먹이를 줄 뿐이다.**
WARP 가 바꾸는 것은 **보간기 셋업을 누가 계산하는가** 하나다.

그러므로 자격검증의 질문은 좁다: **WARP 가 같은 셋업을 만드는가.**

## 1. 참조가 정한 정점 규약 (실측 아님, 소스)

`mgavb.c:88-92`:

```
rhw = win[3]                       Mesa 의 1/w
z   = depth_scale * win[2]         depth_scale = 1/(2^bits − 1)  ->  z ∈ [0,1]
x   =  win[0] + xoffset
y   = −win[1] + yoffset            <- Y 가 뒤집힌다
```

`mga_xmesa.c:364-378`: `depth_scale` 은 16/24/32 비트에 대해
`1/0xffff`, `1/0xffffff`, `1/0xffffffff`.

`mgavb.c:40-41`, `:78-83` (원근 텍스처):

```
tu0 = s,  tv0 = t                  정규화 좌표 그대로 — 텍셀로 스케일하지 않는다
q 가 있으면:  rhw *= q,  tu0 /= q,  tv0 /= q
```

**우리 `OSMGAMesaVertex` 와 그대로 맞는다** — `s`/`tc` 가 이미 *"NORMALISED, not
multiplied by the texture's size"* 이고 `qw`(= Win[3])와 `tq` 를 따로 갖고 있다:

```
rhw = qw * tq
tu0 = s  / tq
tv0 = tc / tq
z   = (z_field / 256) / (2^bits − 1)
y   = 뒤집기 필요
```

**이 변환은 전부 유저랜드에서 double 로 한다.** 커널은 비트 패턴을 검증만 한다.

## 2. 신탁은 소프트웨어가 아니라 **사다리꼴 경로**다

사다리꼴 경로는 이미 소프트웨어와 일치함이 증명됐다. 그러므로 같은 기하를
두 경로로 그려 **픽셀·깊이를 직접 비교**하면 된다. 신탁이 같은 기계 안에 있어
빠르고, 불일치의 원인이 **WARP 의 셋업 하나로 좁혀진다.**

## 3. 물을 것 — 여섯

| | 질문 | 왜 열려 있나 |
| --- | --- | --- |
| **T1** | WARP 의 `z ∈ [0,1]` 이 사다리꼴의 `z0/zdx/zdy` 와 같은 깊이 코드를 내는가 | 우리 실행은 전부 깊이 꺼짐. `z=0.5f` 를 보냈지만 아무도 읽지 않았다 |
| **T2** | `atype = ZI` 로 WARP 가 깊이를 **쓰는가** | `ZI`/`I` 의 뜻은 알지만 WARP 가 그 경로를 먹이는지는 미확인 |
| **T3** | 정규화 `tu0/tv0` 로 실제 텍스처가 나오는가 | 텍스처 레지스터를 전부 0 으로만 돌렸다 |
| **T4** | 원근(`rhw` 가 정점마다 다른 양수)이 참조 규칙대로 되는가 | §1 은 소스일 뿐 실측이 아니다 |
| **T5** | 필터 footprint 가 `CLAMPUV` 안에 묶이는가 | bilinear/mipmap 은 점보다 넓게 읽는다(3-217~218). 사다리꼴에서 실측했지만 WARP 가 좌표를 몰 때도 같은지 |
| **T6** | WARP → 사다리꼴 → WARP 전환이 상태를 오염시키지 않는가 | D2 는 **같은 컨텍스트**가 두 VERTEX 제출 사이에 사는 것만 증명했다 |

## 4. 설계 — 한 부팅에 순서대로, 앞이 뒤를 gate

D2 하네스(오프스크린 블록 + 전수 픽셀 맵 + `DWGSYNC` 펜스 + 실패 정책)를 그대로 쓴다.
블록을 **색과 깊이 두 개** 매핑한다.

```
T1  깊이 읽기      WARP 삼각형 하나, atype I(비교만), ZLT, 미리 채운 깊이 버퍼
                  -> 같은 기하를 사다리꼴로도. 색 픽셀 맵이 같아야 한다
T2  깊이 쓰기      atype ZI, NOZCMP, 삼각형 하나
                  -> 깊이 버퍼를 읽어 사다리꼴 결과와 코드 단위로 비교
T3  텍스처        4×4 알려진 텍스처, nearest + clamp, 사각형(삼각형 2)
                  -> 색 픽셀 맵을 사다리꼴과 비교
T4  원근          같은 사각형, 정점마다 다른 양의 rhw
                  -> 사다리꼴과 비교
T5  필터          bilinear, 텍스처 가장자리를 넘는 좌표
                  -> 텍스처 **밖**에 카나리를 두고 안 읽혔음을 본다
T6  층 전환       WARP -> 사다리꼴 -> WARP, 사이에 펜스
                  -> 세 결과가 모두 맞아야 한다
```

**T1 이 실패하면 T2 이하는 무의미하다** — 깊이 코드 규약이 틀린 것이므로.

## 5. 무엇이 무엇을 뜻하는가

| 관측 | 결론 |
| --- | --- |
| 전부 일치 | WARP 가 사다리꼴과 같은 셋업을 만든다. ABI 를 확정할 수 있다 |
| T1 이 **일정한 오프셋**만큼 다름 | `depth_scale` 규약이 다르다 — 상수 하나로 고칠 수 있다 |
| T1 이 **비선형**으로 다름 | z 가 `[0,1]` 이 아니거나 `rhw` 로 나뉜다(원근 깊이). 규약 재조사 |
| T3 이 **텍셀만큼 밀림** | `tu0` 가 정규화가 아니라 텍셀 단위다 |
| T5 에서 카나리가 읽힘 | **봉쇄 실패** — `CLAMPUV` 가 WARP 경로에서는 다르게 동작한다. 즉시 중단 |
| T6 오염 | 층 전환마다 WARP 그림자를 전부 dirty 로 해야 한다 |

## 6. 봉쇄는 바뀌지 않는다 — 한 곳만 빼고

`TEXCTL` 은 계속 **커널 소유**이고 `CLAMPUV` 는 계속 켜진다. 봉쇄 논증은
그대로다(`HW3D.h:286`). WARP 는 좌표를 만들 뿐 **클램프 하드웨어를 우회하지 않는다**
— 라고 보지만 **그것이 T5 다.** 증명 전까지는 가정하지 않는다.

## 7. 범위 밖

- mipmap(3-217 의 세 번째 footprint) — nearest 와 bilinear 가 먼저다
- 이중 텍스처(파이프 8~15)
- 스텐실
- 성능. 이 단계는 **정확도만** 본다

## 8. codex 에 물을 것

1. §0 이 맞나 — 깊이 비교·`ZI`/`I`·알파·`CLAMPUV` 가 **엔진의 성질**이고 WARP 가
   그것을 바꾸지 않는다는 전제. 반증이 있나.
2. §1 의 정점 규약이 맞나. 특히 `z = depth_scale * win[2]` 가 `[0,1]` 이고
   **원근으로 나뉘지 않는다**는 것. WARP 가 z 를 `rhw` 로 나누나?
3. 사다리꼴 경로를 신탁으로 쓰는 것이 건전한가.
4. T1~T6 의 순서와 gate 가 맞나. 빠진 시험이 있나.
5. T5 의 카나리 배치가 필터 footprint 를 실제로 잡나.
6. WARP 가 깊이를 쓸 때 `MACCESS.zwidth` 와 `ZORG` 정렬(128)이 어떻게 걸리나.
7. 빠진 것.

---

## 9. codex 판정 (2026-08-28) — **NO-GO. 그리고 첫 질문에 정직하게 답했다: 또 일어났다**

| codex 주장 | 검증 | 결과 |
| --- | --- | --- |
| **§1 의 "Y 를 뒤집는다" 가 이 백엔드에서 틀렸다** | `mesa/OpenStepMGAMesaBuffer.c:905` — *"Nothing is flipped either — **OSMesa puts GL row y at base + y \* pitch by default** and the engine draws its rows from the origin the same way"*. `Hook.c:1415` 도 `(dst).y = osmgaFix(Win.data[idx][1])` 로 **그대로** 넘긴다 | ✅**채택 — 내가 틀렸다.** 참조의 뒤집기는 X/DRI 쪽 사정이지 하드웨어가 아니다 |
| `MACCESS = PW32` 가 이미 16 비트 깊이다 | `:543` — *"MACCESS bits 3-4 are a depth-specific width whose **zero value is MA_zwidth_16**, and we only ever write the pixel-width field"* | ✅확인 — 이미 답이 있었다 |
| 큰 오프스크린 Y 는 깊이에 못 쓴다 | `:463` — *"That cannot work for depth: ... at y=1024 the depth access lands 4 MiB above ZORG"* | ✅채택 — T1 기하를 바꿔야 한다 |
| 밉맵은 이미 거절되고, 층 위치는 미측정 | `Hook.c:1993` — *"the four mipmap filters are still refused — where their levels live has not been measured"* | ✅확인 — §7 의 "범위 밖" 이 맞다 |
| **T5 의 카나리가 틀렸다 — 읽어도 안 바뀐다** | 자명하게 참이다. **가져오기를 잡으려면 출력에 주소가 드러나야** 한다. 저장소에 이미 더 나은 기법이 있다(`M1_4D9_MIPMAP_PLAN.md:109` 의 주소 부호화 텍셀) | ✅**채택 — 설계 오류** |
| 4×4 텍스처는 규격 위반 | 사양서가 `twmask/thmask >= 7` 로 **최소 8×8** 을 요구한다 | ✅채택 |
| T1 이 T3~T5 를 gate 하면 안 된다 | 깊이 규약 실패는 깊이 시험만 무효화한다. **의존 분기**로 | ✅채택 |
| **T1 전에 T0(커버리지·서브픽셀)이 필요하다** | D2 는 **정수 좌표 삼각형**만 증명했다. 사다리꼴과의 정확 비교는 부분 커버리지 동등성을 전제한다 | ✅채택 |
| T6 전에 WARP↔사다리꼴을 번갈아 쓰면 안 된다 | 내 T1 이 *"WARP 로 그리고 사다리꼴로도"* 라 이미 전환을 섞는다. **신탁 스냅샷을 먼저 전부 뜨고**, 그 다음 WARP 를 끊김 없이 | ✅채택 — 내 설계 결함 |
| T1 은 **여러 z 와 기울어진 z 평면**이 필요하다 | 한 z 의 삼각형 하나로는 정규화·오프셋·기울기·`rhw` 나눗셈을 구별 못 한다 | ✅채택 |
| T4 는 `rhw` 변화와 **z 변화를 함께** 봐야 한다 | 그것이 *"z 가 원근으로 안 나뉜다"* 의 직접 시험이다 | ✅채택 |
| min/mag 필터를 따로 봐야 한다 | `HW3D.h:433` 에 **이 저장소가 정확히 그 버그를 겪은 기록**이 있다 | ✅채택 |
| `zmode`/`ZI`/`I`/`CLAMPUV` 는 엔진 성질이라는 §0 전제 | 사양서 3-133·3-34·3-212 로 확인. 다만 *"WARP 는 셋업 계산만 바꾼다"* 는 **불완전** — 엣지·서브픽셀·컬링·기울기도 만든다 | ⚖️부분 |

### 9.1 네 번째다

M2 에서 세 번, 여기서 **네 번째**다 — `Buffer.c:905` 는 *"Nothing is flipped"* 라고
**대문자로 적어 놓은** 것을 내가 참조 구현을 보고 뒤집겠다고 썼다.
저장소를 먼저 훑었는데도 **`TEST_STATUS.md` 만 보고 `mesa/` 의 주석은 안 봤다.**
grep 대상이 좁았다.

### 9.2 개정된 순서

```
호스트 전용 사전점검
T0  커버리지·서브픽셀 동등성          <- 정확 비교의 전제
    (신탁 스냅샷을 여기서 전부 뜬다)
T1  깊이 읽기 — 여러 z, 기울어진 평면
T2  깊이 쓰기 (ZI)
T3  nearest clamp 텍스처 (>= 8x8, 주소 부호화)
T4  qw 와 tq 를 독립적으로, z 변화와 함께
T5  bilinear min/mag 각각 + 가드 텍셀로 fetch 주소 확인
T6  전환: 펜스 -> suspend -> quiesce -> 사다리꼴 -> 전체 재발행 -> 재시작
```

**T5 는 카나리가 아니라 가드 텍셀이다** — 텍스처 앞뒤와 행 패딩에 주소가
디코드되는 값을 넣고, 출력에서 그것이 섞여 나오는지 본다. 안 바뀐 메모리는
읽히지 않았음을 증명하지 않는다.

### 9.3 §1 정정

```
z   = depth_scale * win[2],  depth_scale = 1/(2^bits − 1)      ✅ 유지
tu0 = s, tv0 = t  (정규화)                                      ✅ 유지
원근: rhw = qw*tq, tu0 = s/tq, tv0 = t/tq                      ✅ 유지
y   = 그대로                                                    ← 뒤집지 않는다
```

---

## 10. T0 결과 (2026-08-28 22:28:29) — **PASS, 그리고 오라클도 맞았다**

```
M3/T0-oracle: 1176 drawn, 0 wrong (want 1176)
M3/T0-warp:   1176 drawn, 0 wrong (want 1176)
M3/T0:        0 pixels differ between oracle and WARP
M3: T0 PASS -- WARP covers what the trapezoid path covers
```

두 경로가 **기대맵과 정확히 일치**하고 서로도 픽셀 단위로 같다. 자신 없어 했던
사다리꼴 오라클 파라미터도 맞았다. **정확 A/B 비교가 허가됐다** — T1 이하가
의미를 가지려면 필요했던 것.

## 11. T3~T6 — 레지스터 구성은 확보, 그러나 질문이 하나 남는다

사다리꼴 텍스처 경로(`osmgaTextureSetup`, 실기 검증됨)에서 그대로 가져온다:

```
TEXCTL2   = G400_MAGIC | CKSTRANSDIS
TEXCTL    = PITCHLIN | (pitch<<9) | TAKEY | CLAMPUV | TW32
TEXFILTER = ALPHA | (0x10 << 21)
TEXWIDTH  = ((dim-1)<<18) | (((8-log2dim)&63)<<9) | log2dim     (HEIGHT 동일)
TEXTRANS  = TEXTRANSHIGH = 0x0000ffff
DWGCTL    = (GOURAUD & ~OPCODE_MASK) | TEXTURE_TRAP
```

**빠지는 것 둘, 그리고 그것이 요점이다:**

- **`TMR0..TMR8` 을 쓰지 않는다.** 사다리꼴 경로는 좌표 행렬을 CPU 로 계산해
  아홉 레지스터에 싣는다. **WARP 가 그것을 정점에서 만든다** — 이 시험의 전부다.
- **`NOPERSP` 를 켜지 않는다.** 사다리꼴은 affine 평면을 먹이므로 켠다.
  WARP 는 `rhw` 로 원근을 한다. T3 은 `rhw = 1` 이므로 수학적으로 같고,
  T4 가 그것을 `rhw` 를 바꿔 가른다.

### 11.1 미해결 — `TDUALSTAGE0` 이 텍스처에서 무엇이어야 하나

| 근거 | 값 | 맥락 |
| --- | --- | --- |
| 실기 검증된 사다리꼴 텍스처 경로(`osmgaTextureSetup`) | **`0`** | `TEXTURE_TRAP` |
| 사양서 4-42 | **`color0sel = '11'`(`0x00600000`)** | *"Gouraud shaded trapezoids"* |
| 지금의 WARP 목록 | `0x00600000` | 무텍스처, 4-42 를 따름 |

**둘이 충돌하는지, 아니면 4-42 가 `opcode = trap` 에만 걸리는지 모른다.**
`TEXTURE_TRAP` 은 다른 opcode 다. 틀리면 색이 텍스처가 아니라 diffuse 로
나오거나 그 반대다 — hang 이 아니라 그림이 틀린다.

### 11.2 T3~T6 설계

```
T3  nearest + clamp, 8x8 이상, 주소 부호화 텍셀, rhw = 1
    -> 사다리꼴 오라클과 픽셀 비교
    -> TDUALSTAGE0 을 0 과 0x00600000 두 번 돌려 §11.1 을 실측으로 가른다
T4  qw 와 tq 를 독립적으로 바꾸고 z 도 함께 바꾼다
    -> z 가 rhw 로 나뉘지 않음의 직접 시험
T5  bilinear, min 과 mag 각각
    -> 텍스처 앞뒤와 행 패딩에 가드 텍셀(주소 디코드 가능)
    -> 출력에 그 값이 섞여 나오는지로 fetch 주소를 본다
T6  펜스 -> suspend -> quiesce -> 사다리꼴 -> 전체 재발행 -> 재시작
```

### 11.3 codex 에 물을 것

1. **§11.1 이 핵심이다.** `TEXTURE_TRAP` 에서 `TDUALSTAGE0` 은 무엇이어야 하나.
   4-42 의 요구가 `opcode = trap` 전용인가. 두 값을 다 돌려 보는 것이 맞나.
2. `NOPERSP` 를 WARP 에서 끄는 것이 맞나. 켜면 `rhw` 가 무시되나?
3. `TMR0..8` 을 안 쓰는 것이 정말 맞나 — WARP 가 그 레지스터를 **쓰는가**,
   아니면 다른 경로로 텍스처 기울기를 넣는가.
4. T3 의 8×8 주소 부호화 텍셀 설계가 fetch 주소를 실제로 드러내나.
5. T5 의 가드 텍셀 배치(앞·뒤·행 패딩)가 bilinear footprint 를 잡나.
6. 빠진 것.

---

## 12. codex 판정 (2026-08-28) — **NO-GO, 그리고 치명적 누락 하나**

| codex 주장 | 검증 | 결과 |
| --- | --- | --- |
| **WARP 는 `TEXWIDTH/TEXHEIGHT` 인코딩이 다르다** | `mgatex.c:395` 가 문자 그대로 `/* warp texture registers */` 라 적고 `ofs = G200 ? 28 : 11`, `rfw = (10 − s2 − 8) & 63`, `tw = (s2 + ofs) \| 0x40` 을 만든다. python 대조: 8×8 에서 `rfw` 5 vs **63**, `tw` 3 vs **14** — **두 필드가 다 다르다** | ✅**채택 — 치명적.** 사다리꼴 워드를 복사했으면 텍스처가 쓰레기로 나왔다 |
| `WR54/WR62` 는 인코딩된 폭/높이를 받아야 한다 | `mga_state.c:179` 가 `WR54 = texwidth \| WR_MAGIC`, `WR62 = texheight \| WR_MAGIC`. 우리 목록은 `WR_MAGIC` 만 싣는다(`:8473`) | ✅채택. **무텍스처에서는 `texwidth = 0` 이라 우연히 일치** → T0·T1·T2 는 영향 없음 |
| `TDUALSTAGE0 = 0` 이고 **`TDUALSTAGE1` 이 그것을 따라가야** 한다 | 4-42 는 `opcode = TRAP` 절이고 `TEXTURE_TRAP` 은 4-43 의 다른 절이다. 그리고 **저장소가 이미 실측했다**: `M1_4C8_BILINEAR.md:157` — *"홀수 열은 여전히 텍스처의 0xAB 를 가져간다"*, `:159` — **`TDUALSTAGE0/1` 은 직렬 두 단계가 아니라 짝수/홀수 열 레인 둘** | ✅채택 — **다섯 번째다** |
| `TMR0..8` 을 호스트가 안 내는 것은 맞다 | `mga_g400_emit_tex0` 에 TMR 이 없고, Mesa/DRM 어디에서도 CPU 가 쓰지 않는다 | ✅ 확인. 다만 *"WARP 가 그 레지스터를 쓴다"* 는 **증거보다 강한 말** — 챕터 6 이 없어 내부 경로는 미상 |
| `NOPERSP` 를 끄는 것은 맞지만 *"켜면 rhw 무시"* 는 틀리다 | `mgatex.c:373` 의 `texctl` 에 비트 21 이 없다. 그리고 `HW3D.h:383` 이 *"NOPERSP 를 끄면 TMR4/5/8 이 분모 평면이 된다"* 고 적는다 — 분모를 무시하는 것이지 `rhw` 를 안 쓰는 것이 아니다 | ✅채택 |
| 가드 텍셀은 **1 텍셀 2D 헤일로**여야 한다 | `M1_4C8_BILINEAR.md:22` 가 샘플링 규칙 `u' = u·N − 0.5` 를 **32 개 값 전부 일치**로 실측해 뒀다. `u=0` 에서 탭은 `−1`과 `0`, 모서리에서는 넷 중 셋이 밖 | ✅채택 — 네 변·네 모서리 전부 |
| bilinear 에 선형 주소 코드를 쓰지 마라 | 두 연속 코드의 블렌드가 제3의 유효 코드처럼 보인다. 저장소의 **0/255 기저 패턴**이 탭 가중치를 직접 드러낸다(`M1_4C8:9`) | ✅채택 |
| MAG 과 MIN 을 각각 | 둘 다 bilinear 로 두면 어느 선택자도 증명되지 않는다 | ✅채택 |

### 12.1 다섯 번째다

`M1_4C8_BILINEAR.md` 가 **`TDUALSTAGE0/1` 의 레인 구조, 샘플링 규칙, `CLAMP_TO_EDGE`
거동, 0/255 기저 패턴**을 전부 실측해 뒀는데, 나는 §11.1 에서 그중 첫 번째를
*"미해결"* 이라 불렀다. `TEST_STATUS.md` 만 봤고 `M1_4C8` 은 안 봤다.

**대응**: 방금 `TEST_STATUS.md` 에 WARP 여섯 행을 넣었다. 다음 계획 전에는
`docs/` 를 **질문어로 grep** 한다 — 파일 이름을 짐작하지 않는다.

### 12.2 T1·T2 는 영향 없다

이 누락은 전부 **텍스처 레지스터**다. T0·T1·T2 는 텍스처를 0 으로 두므로
`texwidth = 0` 이고 `WR54 = 0 | 0x40 = 0x40` 이라 **지금 설치본이 이미 맞다.**
대기 중인 재부팅은 그대로 유효하다.

### 12.3 T3 이전에 고칠 것

```
warpWidth  = ((w−1)<<18) | (((2−log2w) & 63)<<9) | (((log2w+11) | 0x40) & 63)
warpHeight = 같은 꼴
TEXWIDTH = warpWidth      TEXHEIGHT = warpHeight
WR54 = warpWidth | 0x40   WR62 = warpHeight | 0x40
WR52 = WR60 = 0x40
TDUALSTAGE0 = TDUALSTAGE1 = 0      (TEXTURE_TRAP, 텍스처 대체)
NOPERSP 는 켜지 않는다
```

### 12.4 개정된 부팅 순서

```
T3a  WARP 폭/높이 워드 + WR54/62 교정, nearest, TDS0=TDS1=0, rhw=1,
     고유 ID 텍셀, ARGB 전체와 열 패리티 확인
T3b  TMR 을 범위 안의 틀린 지도로 오염시킨 뒤 반복 (상속 상태 배제)
T4   qw · tq · z 를 각각, NOPERSP 음성 대조 한 밴드
T5a  MAG=BILIN, MIN=NEAREST (확대)
T5b  MIN=BILIN, MAG=NEAREST (축소)
T5c  네 변·네 모서리, 완전한 가드 헤일로, 0/255 기저
T6   WARP 텍스처 -> WARP 무텍스처 -> 사다리꼴 -> 전체 재발행 -> WARP 텍스처
```

---

## 13. T3 상세 설계 (2026-08-28, 코딩 전) — 매핑을 어떻게 같게 만드는가

§12 의 레지스터 수정은 들어갔다(`122ceb0`, 참조 공식과 **바이트 단위 일치**를
호스트 컴파일로 확인). 남은 것은 **두 경로가 같은 매핑을 말하게 하는 것**이다.

### 13.1 두 좌표 규약은 스케일만 다르다

헤더가 적어 둔 사실: *"A full texture spans about `1 << 20` in this fixed
point regardless of [size]"* (`HW3D.h:572`). 그리고 기존 텍스처 프로브는
`step = 1 << (20 − log2dim)` 을 **텍셀 하나**라고 주석한다.

```
오라클(TMR):  픽셀 dx 에서  u = step·dx      ->  u / 2^20 = dx / dim
WARP(정점):   tu = dx / dim                   (정규화)
```

**같은 정규화 좌표다.** 오라클은 2²⁰ 로 스케일해 싣고 WARP 는 그대로 싣는다.

### 13.2 매핑 — 텍셀 하나에 픽셀 하나

텍스처 64×64, 삼각형 다리 48. 삼각형이 텍셀 `0..47` 을 쓰므로 **가드가
16 텍셀 남는다**(T5 의 헤일로가 여기 들어간다).

정점 셋(오른쪽 각이 `(col0,row0)`):

| | tu | tv | IEEE |
| --- | --- | --- | --- |
| v0 | 0 | 0 | `0x00000000` |
| v1 | 48/64 = 0.75 | 0 | `0x3F400000` |
| v2 | 0 | 0.75 | `0x3F400000` |

오라클은 `TMR0 = step`, `TMR3 = step`, 나머지 0, `TMR6/7 = 0` — 기존
`osmgaTextureSetup` 이 이미 그 형태다.

### 13.3 주소 부호화 텍셀

저장소가 이미 쓰는 형태(`:11103`)를 그대로:

```
texel(x,y) = (x << 16) | (y << 8) | 0x40
```

python 확인: 64×64 에서 **4096 개 전부 유일**이고 sentinel `0x5A5A5A` 는
그 집합에 없다(`x = 90` 이 범위 밖). 그러므로 **잘못된 fetch 는 유효한 색이
아니라 잘못된 주소로 디코드된다** — codex 가 요구한 성질이다.

### 13.4 반 텍셀 차이는 실패가 아니라 발견이다

오라클의 TMR 앵커와 WARP 의 정점 보간 사이에 **반 텍셀 규약 차이**가 있을 수
있다. 그러면 전 픽셀이 **일정하게 한 텍셀 밀린 ID** 로 나온다.

**그것은 실패가 아니라 측정된 규약이다.** 판정을 *"오라클과 픽셀 일치"* 하나로
두지 않고, **ID 를 디코드해 (dx,dy) 대비 오프셋을 보고**한다. 균일한 오프셋과
뒤죽박죽은 다른 결론이다.

### 13.5 T3b — 상속된 TMR 배제

codex 권고: WARP 밴드 앞에서 TMR 을 **범위 안의 틀린 지도**로 오염시킨다.
WARP 출력이 그대로면 상속 상태가 배제된다. 범위 밖 값을 쓰지 않는 것이 요점 —
봉쇄를 깨지 않으면서 배제한다.

### 13.6 codex 에 물을 것

1. §13.1 의 스케일 관계가 맞나. `1 << 20` 이 정말 텍스처 크기와 무관한가.
2. nearest 에서 텍셀 선택 규칙이 `floor(u·dim)` 인가 `floor(u·dim − 0.5)` 인가.
   `M1_4C8` 은 **bilinear** 에 대해 `−0.5` 를 실측했는데 nearest 도 같은가.
3. §13.4 의 "균일 오프셋은 발견" 이라는 판정 태도가 맞나.
4. T3b 의 "범위 안의 틀린 지도" 를 어떻게 만들어야 안전하면서 판별력이 있나.
5. 빠진 것.

---

## 14. codex 판정 (2026-08-28) — §13 의 두 곳이 틀렸고, 하나는 시험을 무력화한다

**여섯 번째다.** `M1_4C3_TEXCOORD_PLAN.md` 와 `D3_4_TEXTURE_PLAN.md` 를 안 봤다.

| codex 주장 | 검증 | 결과 |
| --- | --- | --- |
| §13.1 은 64×64 에서 맞다 — **그리고 저장소가 이미 실측했다** | `D3_4_TEXTURE_PLAN.md:247` — `step=0x4000` 으로 **4096/4096 픽셀이 자기 텍셀을 가져갔다**. 사양서 3-228 로도 `tw = log2(width)` → 한 스팬이 정확히 `2^20` | ✅ 확인. 단 NPOT 은 인코딩된 2 의 거듭제곱 스케일을 따른다(`M1_4C3:128`) |
| **nearest 는 `floor(u·dim)` 이지 `−0.5` 가 아니다** | `M1_4C3:13` 이 절단/floor 로 실측. 실제로는 `floor((U + addend)/step)` 이고 addend 가 대역별 511/510/508/504/496(`HW3D.h:590`), 인코더가 496 을 뺀다. 64×64 에서 **0.03 텍셀** — 반 텍셀 규약이 아니다 | ✅채택 |
| **§13.4 의 판정 태도가 틀렸다** — 균일 오프셋은 PASS 가 아니다 | 맞다. T3 은 *"두 경로가 같은 매핑을 말하는가"* 를 묻는다. 오프셋이 있으면 **한쪽을 보정해 정확 일치를 다시 내야** 해결이다 | ✅채택 |
| **"16 텍셀이 T5 가드로 남는다" 가 틀렸다** | 텍셀 48..63 은 **선언된 64×64 안**이다. 그것을 읽는 것은 `CLAMPUV` 를 시험하지 않는다. T5 는 **선언 크기 밖의 물리 헤일로**가 필요하다 — 66×66 할당에 pitch 66, `TEXORG` 가 물리 (1,1) | ✅**채택 — 내 T5 전제가 무의미했다** |
| **경계 정렬 단일 위상이 앵커 오차를 감춘다** | `step·dx` 가 텍셀 경계에 딱 맞으면 서브텍셀 앵커 차이가 floor 를 넘지 않아 통과한다. **T3 이 재려는 바로 그것을 못 잰다** | ✅**채택 — 가장 큰 결함** |
| `base = 8` 로 가장자리에서 떨어뜨려라 | 저 가장자리 클램프가 음의 오프셋을 감출 수 있다 | ✅채택 |
| T3b 오염은 **축 전치 TMR** 이 좋다 | `u = TMR6 + TMR0·dx + TMR1·dy`(`HW3D.h:663`)에서 `TMR0=0, TMR1=step, TMR2=step, TMR3=0` → `(u,v)=(base+dy, base+dx)`. **범위는 그대로**이고 1176 중 대각선 24 픽셀만 우연히 일치 | ✅채택 |
| `TEXFILTER.uvoffseten = 0` 유지 | 3-217: 0 이 OpenGL 샘플링, 1 이 Direct3D | ✅ 확인(현재 0) |

### 14.1 개정 — 위상 밴드가 핵심이다

```
base = 8, 텍셀 8..55 를 쓴다 (양쪽에 8 텍셀 여유)
정점 tu/tv = 8/64, 56/64
위상 밴드 넷:  base + {0, 1/4, 1/2, 3/4} 텍셀
  -> 디코드한 (du,dv) 를 히스토그램으로. 이것이 WARP 앵커를 직접 재는 방법이다
판정: 내부에서 (du,dv)=(0,0) 이어야 PASS.
      균일 오프셋 = 미해결(보정 후 재실행), 변동 = 결함
U 와 V 를 따로, 그리고 짝수/홀수 열 히스토그램도 따로
```

### 14.2 T5 는 물리 헤일로가 필요하다

```
할당 66x66, pitch 66, 선언 크기 64x64, TEXORG = 물리 (1,1)
-> 클램프되지 않은 x = -1/64 이 초기화된 헤일로 메모리에 떨어진다
```
선언 안의 텍셀을 가드로 쓰는 것은 `CLAMPUV` 를 전혀 시험하지 않는다.

## 15. 재부팅 병목을 없앴다

M3 를 **`OSMGAWarpQual` 파라미터로 런타임에 돌릴 수 있게** 했다.
프로젝트가 이미 쓰는 기법이고 이유도 적혀 있다 —
`OSMGA_HW3D_TUNE_PARAM` 의 주석: *"let a measurement turn one thing at a time
on a running machine **rather than on a reboot**"*.

부팅 때와 같은 설정 플래그로 게이트한다. 실패 정책은 그대로 —
wedge 는 어차피 들었을 재부팅을 쓰고, **통과는 재부팅을 0 회 쓴다.**

**다음 재부팅 한 번 뒤로는 M3 반복에 재부팅이 필요 없다.**

---

## 16. T5 의 물리 헤일로 배치 (2026-08-28, python 확인)

§14 가 잡은 것: **선언된 텍스처 안의 텍셀을 가드로 쓰는 것은 `CLAMPUV` 를 전혀
시험하지 않는다.** 헤일로는 **선언 크기 밖**에 물리적으로 있어야 한다.

제약 둘이 부딪힌다 — 헤일로는 `TEXORG` 를 텍스처의 (1,1) 로 밀어야 하는데
`TEXORG` 는 **32 바이트 정렬**이어야 한다. (1,1) 오프셋은 `(pitch+1)·4` 이므로
`pitch ≡ 7 (mod 8)` 일 때만 성립한다. python 으로 전수:

```
pitch 66  ->  268 바이트,  32 로 나눠떨어지지 않음
pitch 71  ->  288 바이트,  나눠떨어짐        <- 이것
pitch 79  ->  320 바이트,  나눠떨어짐
```

### 16.1 배치

```
할당      71 x 66 텍셀 = 18,744 바이트
pitch     71 (텍셀; 선언 폭 64 를 넘어도 된다 -- 헤더가 그렇게 적는다)
선언 크기  64 x 64
TEXORG    base + 288 바이트  = 물리 텍셀 (1,1)
```

클램프되지 않은 좌표가 어디로 떨어지는지, 전부 할당 안임을 확인:

| 논리 좌표 | 물리 | 할당 안 |
| --- | --- | --- |
| `(-1, 0)` | `(0, 1)` | ✅ |
| `(64, 0)` | `(65, 1)` | ✅ |
| `(0, -1)` | `(1, 0)` | ✅ |
| `(0, 64)` | `(1, 65)` | ✅ |
| 좌상 모서리 `(-1,-1)` | `(0, 0)` | ✅ |
| 우하 모서리 `(64,64)` | `(65, 65)` | ✅ |

**네 변과 네 모서리가 전부 초기화된 헤일로 메모리에 떨어진다.** 그러므로
클램프가 듣지 않으면 출력에 헤일로 값이 섞여 나오고, 듣는다면 가장자리 텍셀이
복제된다 — 둘은 구별된다.

### 16.2 그래도 증명하지 못하는 것

가드 색은 **바깥 텍셀이 출력에 기여하지 않았음**을 보일 뿐,
텍스처 캐시가 바깥 물리 주소를 투기적으로 읽고 버리지 않았음은 보이지 못한다.
`HW3D.h:289` 의 *"fetched ADDRESS"* 표현은 색만 보는 실험이 지탱할 수 있는
것보다 강하다 — codex 의 지적이고, 맞다. T5 의 결론은 그만큼만 주장한다.

### 16.3 아직 구현하지 않는다

T3 가 실기에서 돌기 전에는 T5 를 구현하지 않는다. T3 이 앵커와 매핑을
확정해야 T5 의 좌표를 "가장자리에서 얼마나 벗어나게" 둘지 정할 수 있다.
**배치만 확정해 둔다.**

---

## 17. 사전 판정표 — 결과를 보기 전에 못 박는다

이 세션에서 놀라운 결과를 만날 때마다 해석이 흔들렸다. `wbusy` 타임아웃을
처음엔 "WARP 가 못 그렸다" 로 읽었고(실제로는 그려 놓고 판정이 틀렸다),
`dwgengsts` 를 완료로 쓸 수 있는지도 결과를 보고 나서야 정리했다.

**그래서 실행 전에 적어 둔다.** 아래 해석은 로그를 보기 전에 고정된 것이고,
여기 없는 결과가 나오면 **그 사실 자체를 먼저 기록한 뒤** 해석한다.

### T1 — z 규약

| `drew` | 뜻 | 다음 |
| --- | --- | --- |
| `136 0 136` | **z 는 `[0,1]` 정규화.** 참조 규약이 맞다 | ABI 에 `z = win[2]/(2^bits−1)` 확정 |
| `136 136 136` | z 가 코드 단위로 읽힌다 | 클라이언트 변환을 `win[2]` 그대로로 바꾼다 |
| `0 0 0` | 깊이 비교가 전부 거절 — z 가 예상보다 **크다** | `ZLT` 대신 `ZGT` 대조군 필요 |
| `136 0 0` | 프로브 2(대조군)가 프로브 0 과 다르다 | **위치 의존** — 기하나 클립 문제이지 z 문제가 아니다 |
| 그 밖 | 표에 없다 | 먼저 기록하고, 해석은 그 다음 |

### T2 — `ZI` 가 WARP 로도 깊이를 쓰는가

| `depth inside` | 뜻 |
| --- | --- |
| `min == max ≈ 49152` | **쓴다. 그리고 0.75 가 정규화로 앉는다** |
| `min == max == 0` | `ZI` 가 WARP 경로에서 깊이를 안 쓴다 |
| `min != max` | 평면이 기울었다 — 정점 셋이 같은 z 인데 보간이 상수가 아니다 |
| 바깥이 0 이 아님 | **봉쇄 실패** — 즉시 중단 |

### T3 — 텍스처와 앵커

| 네 위상 | 뜻 |
| --- | --- |
| 전부 `exact 1176` | **앵커 일치.** 매핑 규약이 참조와 같다 |
| 전부 같은 좁은 `du/dv` | 균일 오프셋 = **보정할 규약**. 보정 후 재실행해야 해결 |
| 위상별로 갈림(2-2 등) | **서브텍셀 앵커 차이.** 갈리는 지점이 오차 크기를 준다 |
| 넓은 `du/dv` 범위 | 기울기·셋업 결함 |
| `malformed` 다수 | 텍스처 상태가 틀렸다(포맷·피치·`TEXORG`) |

**주의**: `exact 1176` 이 네 위상에서 다 나오는 것이 PASS 다.
*한* 위상만 맞는 것은 PASS 가 아니라 **앵커가 그 위상에 우연히 맞은 것**이다.

### 실패 시 공통

- `*** REBOOT REQUIRED ***` 가 보이면 **링·마이크로코드 보유, 엔진 claim 유지**.
  그 상태로 로그를 다 읽고 나서 재부팅한다 — 지난번 이 순서 덕에 삼각형이
  그려졌다는 사실을 알았다.
- syslog 는 버스트를 버린다. **꼬리의 `SUMMARY` 한 줄이 판정을 담는다.**

## 18. 지금 상태 (2026-08-28 23:1x)

```
설치본  T1 · T2 · T3(4 위상) · OSMGAWarpQual 런타임 트리거
표면    oracle 10M · warp 11M · depth 12M · texture 13M   겹침 0, 정렬 확인
빌드    경고 13(전부 기존)
호스트  check-hw3d-validate: PASS (6 스위트)
머신    T0 부팅 그대로 — T1·T2·T3 미실행
```

**다음 한 걸음은 재부팅이다.** 그 뒤로는 `OSMGAWarpQual` 로 재부팅 없이
T4·T5·T6 을 이어간다.
