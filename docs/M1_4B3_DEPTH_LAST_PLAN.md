# 깊이에 남은 두 가지 — Z1 과 Z3 (계획, 2026-08-21)

## 0. 먼저, 내가 과소보고했다

최근 회차들에서 깊이 잔차를 **−0.504 / −0.518** 로 적었다. 그 숫자는
**잘린 정점 깊이로 만든 평면**에 대고 잰 것이라 **Z1 이 안 보인다.**
정확한 소수 정점 깊이에 대고 다시 재면:

| 도형 | 정점 깊이 | 정확한 평면 대비 | 잘린 평면 대비 |
|---|---|---|---|
| 1 | 57343.125 / 14745.375 / 29490.75 | **−0.921** | −0.504 |
| 2 | 같음 | **−0.937** | −0.518 |
| 3 | 65500 / 200 / 30000 (정수) | −0.525 | −0.525 |
| 4 | 65487 / 61670 / 29285 (정수) | −0.500 | −0.500 |

**두 신탁을 섞어 쓴 내 잘못이다.** 「잘린 평면 대비」는 "엔진이 우리가 준
것을 그대로 칠했나" 를 보는 값이고, 「정확한 평면 대비」가 "규칙에서 얼마나
벗어났나" 다. 앞으로 깊이 잔차는 **정확한 평면 대비**로 적는다.

그래서 남은 것은 둘이고, 각각 반 코드쯤이다:

- **Z1** — 훅이 정점 깊이의 소수부를 버린다. `−0.42`
- **Z3** — 엔진이 출력에서 아래 15 비트를 버린다. `−0.50`

## 1. Z3 — 색·알파와 같은 한 줄

색과 알파는 `osmgaStartFixed` 안에서 시작값에 **반 단계**를 더해 엔진의
절단을 반올림으로 바꿨다. 깊이는 그 함수를 안 거치고 `osmgaFixed` 로 직행해
남았다.

```c
t->z0 = osmgaFixed(at) + (1L << 14);
```

**넘침을 확인해야 한다** — 색은 `255.5 × 32768 = 0x7FC000` 로 여유가 있었지만
깊이는 `65535 × 32768 = 2147450880` 이고 2^31 은 `2147483648` 이다. 반 코드는
`16384` 이므로 `2147467264` — **들어간다. 여유 16384.** python 으로 확인하고
경계 사례를 시험에 넣는다.

## 2. Z1 — 좌표와 달리 **예산 문제가 없다**

좌표의 소수부는 AR 걷기를 거치므로 레지스터 폭에 묶였다. **깊이는 다르다** —
평면이 `DR` 레지스터로 직행하고 `z0`·`zdx`·`zdy` 는 double 에서 15 비트
고정소수점으로 변환될 뿐이다. 그러므로 정점 깊이의 소수부를 **그냥 실으면
된다.** 실을 수 있는 비트를 고를 필요도, 삼각형마다 정할 필요도 없다.

방법: 정점의 `z` 를 좌표와 같이 고정소수점으로 만든다(`1/256`). 훅이
`osmgaSnap` 대신 `osmgaFix` 를 쓰고, 평면 계산이 `(double)v->z / 256` 을 쓴다.

## 3. 순서와 예측 (숫자를 미리 적는다)

**하나씩.** 색에서 A 와 B 를 따로 해서 가운데 열이 확인을 준 것과 같은 이유다.

| | 지금 | Z3 뒤 | Z3+Z1 뒤 |
|---|---|---|---|
| 도형 1 (소수 깊이) | −0.921 | **−0.42 근처** | **0 근처** |
| 도형 2 (소수 깊이) | −0.937 | **−0.44 근처** | **0 근처** |
| 도형 3 (정수 깊이) | −0.525 | **0 근처** | **0 그대로** |
| 도형 4 (정수 깊이) | −0.500 | **0 근처** | **0 그대로** |

**도형 3·4 가 대조군이다** — 정점 깊이가 정수라 Z1 이 없고, 그러므로
Z1 을 고쳐도 **움직이면 안 된다.**

## 4. 그 밖의 기준

- **커버리지가 한 화소도 변하지 않아야 한다.** 깊이 값은 `GL_LESS` 를
  통해 커버리지에 닿을 수 있다(도형 3 이 실제로 그랬다). 7 도형 스윕이
  「nothing to report」.
- **클램프 발동 0 유지.** 반 단계를 더하면 상한에 더 가까워진다 — 계기로
  계속 센다.
- **정수 정점 불변**(호스트 410 삼각형)이 계속 0 행 차이.
- **소수 깊이 시험 도형이 필요하다.** 도형 1·2 가 이미 그렇다(우연히) —
  이번에는 **의도해서** 그렇다고 적어 둔다.

## 5. 안 하는 것

- 깊이 시험의 **동률** 동작(`GL_LEQUAL` 경계). 값이 0 이 된 뒤에 볼 일이다.
- 클램프 상한을 `65535.99997` 까지 넓히는 정리.

## 6. 교차검토 판정 (2026-08-21) — 전건 재검증

| codex 주장 | 검증 방법 | 판정 |
|---|---|---|
| 부호 있는 상한은 `2147483647` 이라 여유는 16384 가 아니라 **16383** | python 산술 | ✅ **채택**. 내가 하나 틀렸다 |
| **기울기 무클램프는 안심이 아니라 경보다** | 독립 측정: 무작위 삼각형 299939 개 중 **911 (0.30%)** 에서 `\|dz/dx\| > 65536/화소` → `(long)` 캐스트가 **정의되지 않음** | ✅ **채택. 그리고 이것이 Z1·Z3 보다 급하다** |
| Z1 한계는 "반 코드"가 아니라 **`(-1, 0]`** | 앞선 측정: 3947 삼각형에서 덮인 화소 최대 **0.9844 코드** | ✅ **채택**. −0.42 는 평균이고 한계는 1 코드다 |
| 반 단계를 더하는 것은 **동률 반올림 규칙을 고르는 일**인데 "동률은 안 본다"와 충돌한다 | 논리 확인 | ✅ **채택**. 규칙을 고르는 것이 맞고, 그렇게 적는다 |
| 대조군(도형 3·4)이 안 움직인다는 예측이 **표 안에서 모순** | 표를 다시 읽음: `−0.525 + 0.5 = −0.025` 가 곧 "0 근처"이고 Z1 무동작이면 그대로 −0.025 | ❌ **기각**. codex 가 "0 근처"를 "정확히 0"으로 읽었다 |
| 누산기 **전 구간** 안전성이 증명 안 됐다 | 시작 화소는 이제 **반드시 덮인 화소**다(`M1_4B2` 의 빈 행 건너뛰기). 그러므로 걷는 값이 전부 삼각형의 깊이 범위 안이고 상한은 `65535 + 0.5` | ⚖️ **부분채택**. 결함은 아니지만 **한계를 적어 둔다** |
| 대조군이 유지되려면 훅 입력이 **정확히** 정수로 스냅돼야 한다 | `osmgaFix` 는 최근접 `1/256` 로 반올림한다. 관측된 깊이 잡음은 `9.8e-4` 로 `1/512` 보다 훨씬 작아 정수는 정확히 `256n` 이 된다 | ⚖️ **부분채택**. 성립하지만 **조건을 명시**한다 |
| 「커버리지 불변」은 far 클리어에서는 공허하다 | 맞다 — 아무것도 거절되지 않는다 | ⚖️ **부분채택**. 기하 커버리지 검사는 유지하고, **층을 쌓은 검사**를 더한다 |

### 6-1. 순서를 바꾼다 — 무클램프 기울기가 먼저

`t->zdx = osmgaFixed(zplane->dx)` 에 클램프가 없다. sliver 삼각형에서
`dz/dx` 가 화소당 500 만까지 가고, `× 32768` 은 `1.66e11` 이라 `(long)`
캐스트가 **정의되지 않는다.** 색은 `osmgaClampSlope(±255)` 로 막혀 있고
그 이유가 주석에 적혀 있다 — **깊이만 빠져 있었다.**

정밀도 문제가 아니라 **정의되지 않은 동작**이고, 평범한(다만 얇은) 기하에서
0.30% 로 닿는다. **Z1·Z3 보다 먼저 고친다.**

한계값: `|dz| ≤ 65535` 이면 `osmgaFixed` 가 `2^31` 안에 든다. 색이 255 로
자르듯 깊이는 **65535 로 자른다** — 화소당 깊이 범위 전체를 건너뛰는 기울기
너머는 어차피 보간할 내부가 없는 삼각형이다.

### 6-2. 그래서 순서

1. **기울기 클램프** (정의되지 않은 동작). 이번 회차.
2. **Z3** — 반 단계. 동률은 **round-half-up 을 고르는 것**이라고 적는다.
3. **Z1** — 정점 깊이의 소수부. 대조군 조건을 명시하고, 오차 한계는
   "평균 −0.42, 한계 1 코드" 로 적는다.
